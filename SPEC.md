# SPEC.md — 文讀 현행 기능·데이터 계약

> **확인 기준: 2026-09-20, 앱 2.4.1**. 기능·저장 계약은 2.4.0과 같으며 2.4.1은 문서 정리와 스타일 생성 입력 경계를 보완한다. 실제 제품 커밋·검증 근거는 [출시 기록](https://github.com/river11456/mundok/releases/latest)과 [현재 인계](PROGRESS.md)에서 확인한다.
> 기존 문서의 “DocJSON v2”는 v2 아키텍처 명칭이다. 실제 저장 모델은 `schemaVersion: 3`, 전체 백업은 `version: 3`, 앱 버전은 `2.4.1`으로 서로 역할이 다르다.
> UI 기준은 [DESIGN.md](DESIGN.md), 목표와 미결정 사항은 [ROADMAP.md](ROADMAP.md), 작업·검증·버전 규칙은 [PROCESS.md](PROCESS.md)다. 과거 세션은 [Git의 과거 세션 기록](https://github.com/river11456/mundok/blob/b1c50083f414d0ad791137cf32642101a34f2b1a/PROGRESS-archive.md), 공식 작업 상태는 [Issues](https://github.com/river11456/mundok/issues)에서 관리한다.
> 이 문서는 코드에서 확인한 현재 계약을 설명한다. 실제 기기·사용자 데이터 검증 완료를 뜻하지 않으며, 관련 근거는 [#5](https://github.com/river11456/mundok/issues/5)에서 확보한다.

---

## 1. 아키텍처 원칙과 현재 범위

1. **문헌의 세계는 두 곳뿐이다** — **카탈로그**(관리자가 큐레이션해 배포하는 저장소, git/Pages)와 **유저 공간**(현재 기기·브라우저 로컬).
2. **새 문헌의 일반 유입 경로는 두 가지다** — ① 직접 생성 ② 카탈로그에서 받기. 이와 별개로 전체 백업 복원·이전과 온보딩 스타터 설치가 있다.
3. **유저 공간의 문헌은 전부 동등하다** — 출처 무관 같은 스키마, 같은 편집 경로(id 직접 수정), 같은 권한. `source`는 "어디서 왔나 + 업데이트 확인용" 꼬리표일 뿐 특별 취급의 근거가 아니다.
4. **저작 모드는 없다** — 관리자도 앱에서는 유저다. 카탈로그 콘텐츠는 유저 공간에서 만들고 다듬은 뒤 **승격**(내보내기 → `catalog/` 커밋)으로 배포한다 (5절).
5. **데이터는 3계층으로 분리된다** — Content(문헌·사용자 수정·폴더) / Progress(학습 로그·세션) / Preference(UI 설정). 카탈로그 원본만 다시 받을 수 있고, 직접 만든 내용·수정·학습 기록은 백업 없이 복구할 수 없다.
6. **계정·서버 동기화는 미래 검토 대상이다** — 현재는 `LocalStore`만 있다. 도입 시 인증·충돌 처리·삭제·이전·동기 호출 계약을 설계해야 하며, 저장 구현 하나로 완료된다고 가정하지 않는다([ROADMAP](ROADMAP.md)).

파생 결과: 실행 모드는 **정적 PWA 단일**이다. `/api/version` 프로브·`ServerStore`·`server.py` 편집 API·`문독.command`는 폐지됐다 (7절).

---

## 2. 도메인 모델

### 2.1 엔티티

| 엔티티 | 내용 | 계층 | 소유 |
|---|---|---|---|
| **Doc** | 문헌 — 메타 + 4레벨 카드 | Content | 유저 (설치본 포함) |
| **Card** | 학습 단위 — 원문·뜻·독음·문법·해석 순서·드릴 링크 | Content | Doc에 내장 |
| **Collection** | 문헌을 담는 `parentId` 폴더 트리 + 순서 — 문헌 위치는 1곳 | Content(조직화) | 유저 (카탈로그 선반은 1회 시드) |
| **ReviewLog** | 학습 이벤트 추가 기록(오래된 구간은 요약 압축 가능) | Progress | 유저 |
| **Session** | 마지막 학습 위치·streak | Progress | 유저 |
| **Prefs** | 선반 접힘·온보딩 등 UI 상태 | Preference | 유저 |

### 2.2 id 체계

- **docId** — 유저 스코프 안에서 유일하면 된다 (현재는 기기·브라우저의 로컬 공간이며 다사용자·다기기 충돌 정책은 미구현).
  - 카탈로그 설치본: **catalogId(한글 slug)를 그대로 docId로** — 설치·업데이트와 학습기록 연결의 기준이다.
  - 직접 생성: `u-` + 난수 8자. 이전 데이터의 `u{n}` ID는 그대로 보존한다.
- **cardId** — 문헌 내 유일·**불변**. 승격돼도, 업데이트로 돌아와도 같은 id를 유지한다 — 학습기록 보존과 업데이트 머지의 기반. 신규 카드는 난수 채번 (순번 채번 폐기).
- 전역 참조 = `docId/cardId` 조합. 신규 채번에 합성 텍스트 id(`${docId}_${type}_${text}`)를 쓰지 않지만, 이전 데이터의 ID는 보존한다.

### 2.3 저장 문헌 스키마 (`DocJSON` / `CardJSON`)

```jsonc
{
  "schemaVersion": 3,          // 현재 문헌 모델 버전. 앱·백업 버전과 역할은 별개
  "id": "기혈다소",
  "title": "…", "sub": "…", "color": "…", "order": 1,
  "source": {                   // 카탈로그 설치본만 존재
    "catalogId": "기혈다소", "version": 2, "installedAt": "2026-09-20T00:00:00.000Z",
    "removed": []               // 사용자가 삭제한 카드 ID: 업데이트 때 재도입하지 않음
  },
  "origin": {                   // 입력 문헌에 포함된 원전 메타. 승격 시 이 값을 보존
    "work": "醫學入門", "author": "李梴", "pages": "12–14"
  },
  "levels": { "char": [], "word": [], "sentence": [], "paragraph": [] },
  "updatedAt": "2026-09-20T00:00:00.000Z"
}
```

```jsonc
// CardJSON (저장형). 런타임 Card는 text→front, meaning→back으로 변환한다.
{
  "id": "c-x7k2",
  "text": "不治已病治未病",
  "meaning": "이미 병든 것을 다스리지 않고…",
  "note": "",
  "reading": "불치이병치미병",
  "grammar": [], "interp": [],        // 전부 카드 내장 — 델타 저장소 없음
  "drill": ["c-a1b2"],                // 선택적 저장 필드. 현재 화면의 드릴다운은 텍스트 매칭(6절)
  "editedAt": 1690000000000,           // 옵션 — 유저 수정 마킹, 업데이트 머지 판단 기준 (C6)
  "status": "draft"                    // 예약 필드. 사진·PDF 인식 기능은 미구현(#8)
}
```

- `schemaVersion`이 없는 구 문헌도 읽으며 신규 쓰기 경로는 3을 기록한다. 형식의 기준은 `src/types.ts`다.
- 현재 카탈로그 index는 문헌 메타·개정 번호·카드 수를 담는다. 독음·뜻 **완성도 계산·표시는 미구현**이다. 콘텐츠 측정과 검수 범위는 [#6](https://github.com/river11456/mundok/issues/6)에서 정한다.
- `status: draft`는 예약돼 있지만 사진 인식·검토 UI는 없다. 수업 날짜·주차 필드도 현행 DocJSON에 없다. 관련 후보는 [#8](https://github.com/river11456/mundok/issues/8)이다.

### 2.4 ReviewLog 스키마

```jsonc
// ReviewEvent: 문헌별 로그. lv는 char | word | sentence | paragraph.
{ "t": "anki", "card": "c-x7k2", "lv": "sentence", "ts": 1690000000000, "grade": 2 }
{ "t": "seq", "card": "c-x7k2", "lv": "sentence", "ts": 1690000000000 }
{ "t": "legacy", "card": "c-x7k2", "lv": "sentence", "ts": 1690000000000, "fails": 3 }
{ "t": "reset", "lv": "sentence", "ts": 1690000000000 }
```

- 오답 수는 `src/review-log.ts`에서 계산한다. `legacy.fails`와 안키 `grade: 1`을 합산하며 해당 레벨의 `reset` 이후 기록을 사용한다.
- 구 오답 수는 `legacy`로 승계한다. 카드가 빈 문자열인 legacy는 과거 시각만을 나타낼 수 있다.
- 로그는 이벤트를 추가하고, 길이가 4,000개를 넘으면 오래된 구간을 legacy 요약으로 접고 최근 2,000개를 유지한다. 세부 이력이 영구 보존되는 것은 아니다.
- 날짜 기반 복습 큐·장기 통계 대시보드는 없다. 홈 연속 학습 표시와 결과 화면의 오늘 학습·오답 요약은 있다.

---

## 3. 기능 명세 (엔티티 기준)

### 3.1 학습·복습

| # | 기능 | 트리거 | 읽기 | 쓰기 |
|---|---|---|---|---|
| L1 | 서가 홈 | 홈 진입 | Doc*, Collection, Session, Prefs | — |
| L2·L3 | 이어서 학습 | 히어로 | Session | Session |
| L4 | 문헌 상세 오버레이 | 표지 클릭/숫자키 | Doc, Collection(참고문헌) | — |
| L5·L6 | 순차 재생 / 안키 | 오버레이 `Enter`/`⇧Enter` | Card, ReviewLog(파생) | Session, ReviewLog |
| L7~L11 | 뒤집기·평가·결과·재시작·초기화 | 기존 단축키 동일 | Card, ReviewLog | ReviewLog, Session |
| L12 | 드릴다운 | 밑줄 클릭 | 하위 레벨 카드의 원문 매칭 → 대상 ID(6절) | — |
| L13~L15 | 독음·문법·해석 순서 | 자동/`G`/▶ | `card.reading·grammar·interp` | — |
| L16~L18 | 폴더 탐색·온보딩·도움말 | — | Collection, Prefs | Content, Prefs |

### 3.2 저작 — 단일 경로 (모든 문헌 동일)

| # | 기능 | 트리거 | 동작 |
|---|---|---|---|
| A1 | 카드 추가 | 셀 스윕 → 버블 | Doc에 카드 삽입 (id 채번). 없던 하위 레벨의 첫 카드면 런타임 문헌 구조도 즉시 재동기화해 드릴다운 링크 생성 |
| A2~A4 | 카드 수정·연결 일괄수정·삭제 | 카드 도구 | **id로 직접 수정** — 원문을 고쳐도 문법·해석·학습기록이 카드 id에 붙어 있어 고아화 없음 |
| A5·A6 | 문법·해석 순서 편집 | 文 메뉴 | 카드 내장 필드 직접 수정 |
| A7 | 문장 학습 편집기 | 문장 카드 수정 | 큰 학습 모달에서 원문·한자별 독음칸·해석·메모 편집. 폰에서는 전체 높이 시트. 문법·해석 순서는 기존 도구 유지 |
| A8 | 독음 입력 지원 | 독음칸 입력·붙여넣기·자동 입력 | 부분 입력·IME 조합·전체 독음 분배, 로컬 카드·사전 후보로 빈칸 채우기. 기존 입력 보존 |
| A9 | 한자 사전 지원 | 한자 선택·추가 | 내장 사전의 대표·대체 후보와 Daum 확인 링크. 기존 카드의 빈 독음·뜻만 보강하고 중복 생성을 피함 |

- v1의 분기 3종(텍스트 델타 / user-docs 직접 / 서버 API)이 **한 경로로 통합**됐다. `docs-merge.ts`의 "알려진 한계"(텍스트 수정 시 주석 고아화)는 구조적으로 소멸.

### 3.3 수급

| # | 기능 | 동작 |
|---|---|---|
| C1 | 새 문헌 만들기 | 3단 마법사 (현행 유지) → 유저 공간에 생성 |
| C2~C4 | 본문 추가·정보 수정·삭제 | 모든 문헌에서 가능 (설치본 포함 — 공리 3) |
| C5 | 문헌 받기 | 카탈로그 → 유저 공간 설치 (+`source`). 폴더 안에서 신규 설치하면 카탈로그 창을 연 폴더에 배치하고, 업데이트는 기존 위치 유지 |
| C6 | 업데이트 | 카드 **id 단위 머지** — `editedAt` 붙은 카드는 유저 것 보존, 나머지는 상류 개정 적용, 종료 시 "N장은 직접 수정분 유지" 요약 알림. base 스냅샷 불필요. 사용자 삭제 카드도 재도입하지 않는다. 카드별 비교·선택 적용은 미구현 후보 [#10](https://github.com/river11456/mundok/issues/10). v1의 "통째 교체·수정분 소실" 폐기 |
| C7 | JSON 내보내기 | 문헌 상세 → DocJSON v2 파일 다운로드 — 승격 파이프라인의 입구 (5절) |
| C8 | 스타터 제안 | 온보딩 마지막에 `불치이병치미병` 1문헌 **원클릭 설치 제안** — 카탈로그 정본의 정적 import를 사용해 추가 다운로드 없이 설치. 빈 서가 첫인상 회피 + 선택권 존중 |

### 3.4 조직화 — 사용자 소유 (구 P8 해소)

| # | 기능 | 동작 |
|---|---|---|
| O1 | 폴더(컬렉션) | **2.3.0 구현, 2.3.1 UI 정리** — `parentId` 트리로 임의 깊이 저장, 폴더 진입·breadcrumbs·이름변경·삭제·문헌 이동. 폴더는 카드 안 아이콘이 아니라 탭 달린 폴더 실루엣 자체로 표시한다. 생성 진입점은 홈 toolbar의 단일 **추가** 메뉴이며, 현재 메뉴·생성 진입 UI는 루트 아래 2단계로 제한한다. 임의 중첩 목표와 현재 제한의 차이는 [#4](https://github.com/river11456/mundok/issues/4)에서 해소한다. 삭제 시 문헌·하위 폴더를 부모로 승격해 보존. 카탈로그 컬렉션은 최초 1회 시드 후 **완전 사용자 소유** (`mundok-v3/collections`) |
| O2 | 참고문헌 관계 | 구 `_groups.refs` → 카탈로그 컬렉션 메타(`_collections.json`)로 이전 — 계속 카탈로그 소유. 폴더 명시 배치가 자식 숨김보다 우선 |
| O3 | 홈 정렬 | 현재 위치의 직계 하위 폴더(컬렉션 배열 순서) → 현재 폴더 문헌(`docIds` 순서). 라이브러리 루트의 미배치 문헌은 카탈로그 order → 생성순. 새 문헌은 생성 당시 보고 있던 폴더에 자동 배치 |

### 3.5 백업 — 포맷 v3

```jsonc
{ "version": 3, "exportedAt": …,
  "content":    { "docs": [], "collections": [] },
  "progress":   { "logs": {}, "session": { "last": …, "streak": … } },
  "preference": { … } }
```

- 현재 UI는 덮어쓰기 확인 후 전체 데이터를 복원한다. Progress만 선택하는 부분 복원 UI는 없다.
- **v3·구 v2 키 덤프·구 userdata 단일 객체 가져오기**를 지원한다. 구 형식은 v1 키를 복원한 뒤 새로고침 시 9절 이전 경로를 사용한다.
- collections를 담지 않은 구 백업은 다음 로드에 1회 시드하고, 담긴 폴더는 정규화한다. 실제 데이터 왕복·저장 실패 시 보존·기존 PWA 업데이트 검증은 [#5](https://github.com/river11456/mundok/issues/5)에 남아 있다.

---

## 4. 데이터 저장 위치

### 4.1 git (카탈로그 — 관리자 소유)

| 경로 | 내용 |
|---|---|
| `catalog/*.json` | 배포 문헌 정본 (DocJSON v2). 구 번들 8문헌도 정본은 여기 있으며 일부는 여기서 직접 import(12절) |
| `catalog/_collections.json` | 카탈로그 제공 컬렉션(교재 묶음)·참고문헌 관계 — 구 `_groups.json` 승계 |
| `dist/catalog/index.json` | 빌드 산출 — 문헌 메타·개정 번호·카드 수. 완성도 지표는 없음 |
| `src/data/textbook/_manifest.json` | 기존 교재의 원전·저자·지면 참고 메타. promote는 이 파일을 자동 조회하지 않음 |

### 4.2 localStorage (유저 공간)

| 키 | 내용 | 계층 |
|---|---|---|
| `mundok-v3/docs` | 유저 공간 문헌 전체 (`DocJSON v2[]`) | Content |
| `mundok-v3/collections` | 사용자 폴더 트리 (`{id,name,docIds,parentId?,color?,icon?}[]`) | Content |
| `mundok-v3/log/<docId>` | ReviewLog (문헌별 append) | Progress |
| `mundok-v3/session` | 마지막 위치·streak | Progress |
| `mundok-v3/prefs` | 선반 접힘·온보딩 등 통합 | Preference |

- `Store`가 편집·로그·세션·설정 API를 제공하며 `LocalStore`가 구현한다. 문헌·폴더 래퍼, 백업·이전 코드에도 localStorage 접근이 있다. 저장소 변경 시 이 경계를 함께 검토해야 한다.
- 카탈로그 파일 크기는 8절의 저장소 측정값이며 실제 브라우저 저장 용량을 보장하지 않는다. 사용자 문헌·학습 로그도 함께 저장되므로 실패·용량 경계는 #5 검증 범위다.

---

## 5. 카탈로그 파이프라인 (승격 경로)

```
유저 공간에서 저작·수정 (앱, 일반 기능만 사용)
  → C7 JSON 내보내기 (DocJSON v2)
  → scripts/promote.mjs <file>   # lint + version 증가 + catalog/<id>.json 배치
  → 이슈 연결·앱 버전 결정·PR 검사/검토 → main 병합 → Pages 배포·운영 확인·Release 기록
  → 각 유저: C6 업데이트 (id 머지)
```

- `promote.mjs`는 입력 문헌의 `origin`을 보존하며 manifest를 자동 조회하지 않는다. 출처·이용 근거를 확인하고 필요한 메타를 입력 문헌에 포함한다.
- **C7 내보내기는 모든 사용자에게 열려 있다**. 카탈로그 반영은 저장소 관리 절차를 따른다. 전체 사용자 데이터 백업과 문헌 하나의 JSON 내보내기는 다르다.
- `lint-data.mjs`는 카탈로그 문헌을 검사한다. 실제 명령과 폰트 갱신은 [catalog/README.md](catalog/README.md), 출시 규칙은 [PROCESS.md](PROCESS.md)를 따른다.

---

## 6. 초기화·로드 순서 (v2)

```text
1. initStore()                   LocalStore 초기화
2. purgeV1IfMigrated()           이전 로드에서 생성된 docs·session 키가 모두 있으면 구 키 제거
3. migrateV1IfNeeded(V1_BAKED)    콘텐츠 키가 없으면 구 문헌·델타 이전 또는 빈 공간 생성
4. migrateProgressIfNeeded()     세션 키가 없으면 구 학습 로그·설정 이전
5. seedCollectionsIfNeeded()     폴더 키가 없으면 실존 문헌 기준 최초 1회 시드
6. syncUserDocs() / initGrammar() 런타임 문헌과 문법 상태 구성
```

- 일반 사용에서는 로컬 문헌을 런타임으로 변환한다. 구 텍스트 델타 병합은 이전·구 백업 호환 경로에 남아 있다.
- 드릴다운은 `render.ts`의 하위 레벨 원문 후보와 `drill-match.ts`의 텍스트 구간 매칭으로 계산한다. 저장형의 선택적 `drill` 필드를 런타임이 우선 적용하는 구현은 없다. ID 기반 참조 전환을 완료했다고 간주하지 않는다.

---

## 7. v1에서 폐지·유지한 경로

| 폐지 대상 | 근거 공리 |
|---|---|
| `ServerStore` · `/api/version` 프로브 · 실행 모드 2종 | 4 (저작 모드 없음) |
| `server.py` 편집 API 7종 (`create-doc` `add-card` `edit-card` `delete-card` `save-grammar` `save-interp` `save-groups`) | 4 — 승격 파이프라인으로 대체 |
| `문독.command` 런처 | 4 — 관리자도 배포본(또는 `vite dev`) 사용 |
| `src/data/*.json`·`import.meta.glob` 기반 정본 | 카탈로그로 이전. 카탈로그 8문헌의 정적 import는 이전·스타터에 유지(12절) |
| 텍스트 델타 (`hanja-v2/userdata` — additions·edits·deletions·grammar·interp) | 3 — 마이그레이션 때 설치본에 구워 넣고 폐기 |
| `_groups.json` 서버 편집 (O1 구판) | 3·4 — 조직화는 사용자 소유 + 카탈로그 컬렉션 |
| 신규 합성 카드 id · 순번 채번 | 신규 채번 경로에서 폐지. 이전 ID와 일부 호환 입력 필드는 유지(2.2·12절) |
| `migrateOldAnki` (구구 포맷 변환) | v2 마이그레이션이 최종 상속 |

---

## 8. 콘텐츠 현황 (2026-09-20 저장소 측정)

`catalog/*.json` 중 `_`로 시작하는 메타 파일을 제외하고 모든 레벨의 카드를 집계했다.

| 문헌 수 | 카드 수 | 독음이 비어 있지 않은 카드 | 뜻이 비어 있지 않은 카드 | 해석 순서가 있는 카드 |
| --- | --- | --- | --- | --- |
| 82 | 3,205 | 1,062 | 613 | 1 |

- 문헌 JSON 합계는 749,764바이트다. 구 번들 8문헌도 이 82문헌 안에 포함되며 중복 집계하지 않았다.
- 비어 있지 않은 필드 수이며 정확성·검수 완료율이 아니다. 카탈로그 앱 화면의 완성도 표시도 현재 없다.
- 이전 “73문헌·독음/뜻 0%”는 과거 측정이다. 현재 보강 목록·출처·검수 기준은 [#6](https://github.com/river11456/mundok/issues/6)에서 관리한다.

---

## 9. 마이그레이션 (v1 → v2, 1회)

**보존 목표와 현재 동작**: 콘텐츠 키(`mundok-v3/docs`)와 세션 키(`mundok-v3/session`)를 별도 가드로 삼아 필요한 이전을 수행한다. 실제 데이터 무손실·실패 복구 여부는 #5에서 검증한다.

| 단계 | 내용 |
|---|---|
| 0 | (선행, 코드 아님) **베이킹 8문헌을 카탈로그에 등록** — 이때 기존 카드 순번 id를 **그대로 카드 id로 고정**해 매핑표 없이 학습기록이 이어지게 한다 |
| 1 | 이전 전 백업은 운영 검증의 전제다. 자동 이전 전에 별도 백업 안내 화면을 띄우는 단계는 현재 초기화 코드에 없다 |
| 2 | 베이킹 8문헌 + `userdata` 델타 적용 **결과물**을 유저 공간 설치본으로 굽기 (`source`=카탈로그) — 델타 소멸 |
| 3 | `user-docs` → `mundok-v3/docs` 이전. 기존 문헌·카드 ID는 변환하지 않고 보존 |
| 4 | `fails` 스칼라 → ReviewLog `t: legacy`·`fails` 요약 레코드 (2.4) |
| 5 | `<lv>_ts`·`last-session`·`streak` → `session`, `shelves-collapsed`·`onboarding-seen` → `prefs` |
| 6 | ~~구 `hanja-v2/*` 키는 지우지 않는다~~ → **2.1.1에서 제거 집행** — 마이그레이션이 이전 로드에서 완료된 경우(`mundok-v3/docs`·`session` 선재)에만 다음 로드에서 일괄 삭제(`purgeV1IfMigrated`). 갓 마이그레이션한 로드는 건너뛰어 최소 1로드분 롤백 보험 유지 |
| 7 | 백업 v2 파일 가져오기도 같은 변환 경유 (3.5) |

---

## 10. 이전 설계 이력

v1 구조 문제(P1~P12)의 당시 판단은 [이전 명세](https://github.com/river11456/mundok/blob/b1c50083f414d0ad791137cf32642101a34f2b1a/SPEC.md)에 보존한다. 현재 계약은 이 문서의 1~9절, 남은 목표는 [ROADMAP.md](ROADMAP.md)와 이슈를 따른다. 과거 해소 표·보안·성능 확인을 현재 검증 완료 근거로 사용하지 않는다.

---

## 11. 결정 기록과 대체 관계

기본 결정은 2026-07-31 당시 기록을 이어받았다. 서비스화의 대상·환경·보존·지원 책임은 여전히 [ROADMAP](ROADMAP.md)의 미결정 사항이다.

| 결정 | 채택안 | 기각안 |
|---|---|---|
| id 형식 | 설치본 docId=**카탈로그 id 유지**, 직접 생성 `u-`+난수, cardId는 문헌 내 난수·불변 (2.2) | 전면 난수 — 기록 키 재작성 비용 대비 실익 없음 |
| 업데이트 머지 | **`editedAt` 플래그 기준 수정 카드 보존 자동** + 요약 알림, base 스냅샷 없음 (C6) | 카드별 선택 UI(현재 #10 검토 후보) · 상류 우선(수정분 소실 — 공리 3 위배) |
| 신규 첫 경험 | **온보딩 원클릭 스타터 설치 제안** (C8) | 무단 자동 설치 · 빈 서가 |
| `server.py` | **완전 삭제** — 개발 `vite dev`, 저작 배포본 앱, 승격 `promote.mjs` (7절) | 정적 서버 잔존 — 유지 이유 없음 |
| ReviewLog 필드 | 당시 최소안 `{card, ts, mode, grade?}`는 구현 중 `t`·`lv`·legacy·reset 이벤트로 구체화됐다. 현행은 2.4를 따른다 | 최초 최소안을 현행 저장 형식으로 사용하는 것 |
| 컬렉션·refs 위치 | `catalog/_collections.json`에 구 `_groups` 선반·refs 승계, 유저 컬렉션은 로컬 별도 (3.4, 4.1) | — |
| localStorage 접두사 | `mundok-v3/` (4.2) | — |
| 선반 소유 모델 (2026-08-01 추가) | 카탈로그 선반 **1회 시드 → 사용자 소유** (시드는 유저 공간 실존 문헌만·빈 선반 제외), 문헌 위치 1곳(굿노트형 폴더-파일), '내 문헌'·'미분류' 통합. 소유 모델은 유지하며 열린 선반 시각은 아래 9/3 결정으로 대체 — O1 | 읽기 전용 카탈로그 선반 병존(빼기 불가 선반 발생) · 다중 선반 중복 배치 · 폴더 진입형 기각은 당시 판단이며 **9/3 결정으로 대체** |
| 폴더 탐색 (2026-09-03 대체) | 열린 선반에서 폴더 진입·breadcrumbs·혼합 그리드로 전환. 생성·이름변경·삭제·문헌 이동 구현. 중첩 목표 대비 2단계 생성 UI 제한은 #4에 남음 | 같은 화면의 접힌 선반을 현행 UI로 유지하는 안 |

---

## 12. 호환성 유지와 남은 경계 (2026-09-20)

| # | 편차 | 사유 |
|---|---|---|
| 1 | **기존 id 전면 보존** — 마이그레이션이 `u{n}` 문헌 id와 델타 추가 카드의 합성 id(`${docId}_${type}_${text}`)를 변환하지 않고 그대로 둔다. 새 규칙(`u-`+난수, `nextCardId`)은 **신규 생성에만** 적용 | id를 바꾸면 학습기록 키 전면 리맵이 필요하므로 기존 참조를 유지한다. 2.2의 신규 채번 규칙과 이전 데이터 보존을 구분 |
| 2 | `updatedAt`은 epoch가 아니라 **ISO 문자열 유지** | v1 user-docs 데이터와 필드 호환 |
| 3 | **ReviewEvent 실제 스키마**: `{t:'anki',card,lv,ts,grade}` · `{t:'seq',card,lv,ts}` · `{t:'legacy',card,lv,ts,fails}`(v1 승계, card `''`=시각만) · `{t:'reset',lv,ts}`. 2.4와 `src/types.ts`가 현행 계약이다. 로그 비대 시 `review-log.ts compact`가 legacy 요약으로 접는다 (파생값 불변) | fail_count가 레벨 스코프라 `lv` 필수, 초기화·승계도 이벤트로 표현해야 append-only가 성립 |
| 4 | **마이그레이션용 베이킹 8문헌 번들 유지** — `docs.ts`가 카탈로그 정본 8개를 정적 import (이전에 필요한 문헌을 별도 네트워크 요청 없이 제공). 구 백업 가져오기가 재이전 경로를 사용하고, `불치이병치미병`은 오프라인 스타터에도 사용한다. 제거를 미완료 구현으로 취급하지 않는다 | 별도 정본 복제가 아니라 카탈로그 직접 import. 제거하려면 #5의 호환·오프라인 대체 경로를 먼저 확인 |

- 마이그레이션 가드 2중: 콘텐츠(`mundok-v3/docs` 부재 시) / Progress·Prefs(`mundok-v3/session` 부재 시) — 각각 1회.
- 구 `hanja-v2/*` 키는 **2.1.1에서 제거 집행** (9절 6단계 — `purgeV1IfMigrated`, 마이그레이션 완료 다음 로드에서 삭제). 백업 v2 가져오기는 여전히 이 키를 복원해 재마이그레이션하는 경로로 동작 — 복원분도 다음 로드에서 같은 경로로 청소된다. `migrate-v1.ts` 코드는 이 가져오기 호환 때문에 유지.
- 사용자 폴더 편집 UI는 구현돼 있다. 카드별 상류판 비교 UI는 [#10](https://github.com/river11456/mundok/issues/10) 후보이며 폴더 생성 제한 해소는 [#4](https://github.com/river11456/mundok/issues/4)다.
