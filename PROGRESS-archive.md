# 文讀 — 과거 세션 기록 (2026-06-30 ~ 2026-09-03)

> 2026-09-20에 긴 세션 기록을 모았다. 아래 내용은 각 작성일 당시의 기록이며, 배포·검증 상태를 현재 상태로 해석하지 않는다.
> 당시의 “미배포”, “다음 작업”, 버전·일정·기각 결정은 후속 변경으로 대체될 수 있다. 현재 인계는 [PROGRESS.md](PROGRESS.md), 기능은 [SPEC.md](SPEC.md), 작업 상태는 [GitHub Issues](https://github.com/river11456/mundok/issues)를 따른다.
> 특히 8/1의 폴더 진입형 기각은 9/3의 폴더 라이브러리 구현으로 대체됐고, 9/3의 미배포 기록 이후 변경은 9/20 출시 기준에 반영돼 있다. 아래 로그 본문은 역사 보존을 위해 유지한다.

---

## 📌 세션 (2026-09-03) — 폴더 배치·단어 드릴다운 수정 (v2.3.2)

기존의 한 화면 평면 선반을 **폴더 진입형 라이브러리**로 교체했다. 데이터는 `parentId`로 임의 깊이를 표현하고, 첫 UI에서는 탐색 안정성을 위해 **루트 아래 2단계까지만 새 폴더 생성**을 허용한다. 기존 `mundok-v3/collections` 배열은 `parentId` 없는 최상위 폴더로 그대로 승계된다.

- 홈: 폴더·문헌 혼합 그리드, 폴더 드릴인, 상위 경로 breadcrumbs, 빈 폴더, 폴더 이름 변경·삭제
- UI 정리: 폴더는 흰 카드 안 SVG 아이콘이 아니라 **타일 전체가 탭 달린 폴더 실루엣**으로 보이게 변경
- 추가 흐름: 홈 그리드의 점선 `새 문헌`·`문헌 받기` 타일과 별도 `새 폴더` 버튼을 없애고, breadcrumbs 우측 **`＋ 추가` 메뉴** 하나에서 `새 폴더`·`새 문헌`·`문헌 받기`를 선택
- 깊이 정책: 2단계 폴더에서는 추가 메뉴 안의 `새 폴더`만 비활성화하고, `새 문헌`·`문헌 받기`는 계속 사용 가능
- 보존 규칙: 폴더 삭제 시 문헌과 직계 하위 폴더를 상위 폴더(또는 라이브러리 루트)로 승격해 콘텐츠를 삭제하지 않음
- 문헌 흐름: 현재 폴더에서 만든 새 문헌은 해당 폴더에 자동 배치, 상세의 ‘폴더 이동’에서 전체 경로를 보고 이동
- 배치 수정: 새 문헌·문헌 받기 창을 연 시점의 폴더를 목적지로 고정. 카탈로그 신규 설치도 현재 폴더에 들어가며, 기존 문헌 업데이트는 원래 위치를 유지
- 드릴다운 수정: 없던 단어 레벨에 첫 카드를 추가한 뒤 저장소의 문헌 구조를 즉시 다시 동기화해 문장 → 단어 링크가 바로 생성되도록 수정
- 호환성: 잘못된 부모·사이클·중복 문헌 배치를 보수적으로 정규화하며, 백업 v3 가져오기/내보내기에도 동일 정규화 적용
- 후속 범위: 폴더 색·아이콘 필드는 예약하되 UI는 보류. 검색·정렬·목록 보기·드래그 이동도 후속 작업
- 검증: 전체 테스트 121건, TypeScript 검사·프로덕션 빌드 통과. 폴더 배치·카탈로그 업데이트 위치 보존·첫 단어 레벨 런타임 연결 회귀 테스트 포함. 데스크톱 브라우저에서 폴더 안 추가 메뉴와 두 모달의 경로 유지, 콘솔 오류 0건 확인

### v2.3.2 버그 수정 기록

| 보고 증상 | 원인 | 수정 |
|---|---|---|
| 폴더 안에서 문헌을 받아도 라이브러리 루트에 추가됨 | 카탈로그 설치 흐름이 `S.folderId`를 전달·저장하지 않음 | `events.ts`가 메뉴를 닫기 전에 현재 폴더를 캡처하고, `catalog.ts`가 설치 요청 시작 시 목적지를 다시 캡처. 신규 설치만 `placeCreatedDoc()`으로 해당 폴더에 배치하고 업데이트는 기존 위치 보존 |
| 새 문헌의 위치가 화면 상태에 의존 | 생성 완료 시점의 전역 `S.folderId`를 읽음 | `doc-create.ts`가 생성 창을 연 폴더를 별도 보관하고 비동기 생성 완료 후에도 그 목적지를 사용 |
| 개별 한자 추가는 되지만 첫 단어 추가 후 드릴다운이 생기지 않음 | 저장소에는 `word` 레벨이 생성됐지만 메모리의 `DOCS`가 갱신되지 않아 렌더러가 새 레벨을 찾지 못함 | 저장 성공 직후 `syncUserDocs()`로 정본을 다시 읽고 `resolveRuntimeAddition()`으로 새 레벨·카드를 현재 런타임에 연결. 중복 카드의 세션 재삽입도 방지 |

- 주요 변경 파일: `src/events.ts`, `src/doc-create.ts`, `src/catalog.ts`, `src/addcard.ts`, `src/collections.ts`, `src/card-runtime.ts`
- 회귀 테스트: `test/collections.test.ts`에 폴더 신규 배치·업데이트 위치 보존 2건, `test/card-runtime.test.ts`에 첫 단어 레벨 연결·중복 방지 2건 추가
- 검증 명령: `npm test` **121/121 통과**, `npm run build` 통과, `git diff --check` 통과
- 빌드 참고: 콘텐츠 lint ERROR 0건. 기존 콘텐츠의 드릴다운 미매칭·폰트 폴백 경고는 이번 기능 수정과 별개로 남아 있음
- 실행 상태: 로컬 개발 서버 `http://127.0.0.1:5173/` 실행 중, 브라우저 표시 버전 v2.3.2. 아직 커밋·push·배포하지 않음
- 로컬 재현 데이터: 브라우저 저장소에 `테스트` 폴더와 `再現文獻…`, `測試路徑…` 문헌이 남아 있음. 사용자 데이터이므로 자동 삭제하지 않음
- 남은 정합성 작업: 대화에서 정한 **폴더 무제한 중첩**과 달리 현재 생성 UI에는 2단계 제한이 남아 있음(`canCreateChildFolder`). 데이터 모델은 이미 임의 깊이를 지원하므로 UI 제한 제거가 필요

---

## 📌 세션 (2026-08-01) — 굿노트형 선반 UI: 사용자 선반 CRUD·문헌 이동 (v2.1.2 → v2.2.0)

SPEC 3.4 **O1(사용자 컬렉션 편집 UI — 2.x 예정)을 조기 구현**. 설계 확정(사용자): A안 "열린 선반"(현행 가로 섹션 강화 — 폴더 진입형 기각), 카탈로그 선반 1회 시드 후 사용자 소유, 문헌 위치 1곳, '내 문헌'→'미분류' 통합.

| 커밋 | 내용 |
|------|------|
| v2.1.2 | **데이터 층** — `collections.ts`(순수 로직+`mundok-v3/collections` 래퍼) 신설, 시드(유저 공간 실존 문헌만·빈 선반 제외·키 존재 = 완료), Store에 loadCollections/saveCollections, 백업 v3 `content.collections` 왕복(구 백업은 재시드), 테스트 12건 |
| v2.2.0 | **UI** — `shelvesForHome` 사용자 선반 기준 재작성(빈 선반도 렌더, 명시 배치 > refs 숨김), 선반 헤더 이름변경·삭제 아이콘(hover 노출·터치 상시)·'＋ 새 선반' 바·**널판**(ledge)·접힘 시 **문헌 색칩 미리보기**·빈 선반 점선 슬롯, 상세 오버레이 '선반 이동' → 선택 시트(`shelf-ui.ts` — 현재 위치 체크, '＋ 새 선반으로 이동' 연계), 문헌·선반 삭제 시 배치·접힘 prefs 청소 |

- 검증: 테스트 106건(신규 12)·tsc·lint·빌드 통과. `Doc.userDoc` 플래그·`SHELVES` 정적 의존 제거
- ⚠ push는 아래 v2.1.1 보류 건과 함께 판단 (마이그레이션 실기기 확인 후 일괄 push 권장)

---

## 📌 세션 (2026-08-01) — 구 hanja-v2/* 키 제거 집행 (v2.1.1)

- **2.x 정리 ① 집행** — `purgeV1IfMigrated()` 신설(`migrate-v1.ts`): 마이그레이션이 **이전 로드에서 완료**된 경우(`mundok-v3/docs`·`session` 선재)에만 `hanja-v2/*` 전량 삭제. 갓 마이그레이션한 로드는 건너뛰어 **최소 1로드분 롤백 보험 유지**. `initDocs`에서 마이그레이션보다 먼저 호출
- 부수 정리: `deleteUserDoc`의 구 키 스윕 제거(purge가 전량 처리, stale 주석 정정). **`migrate-v1.ts` 코드는 유지** — 백업 v2 가져오기가 구 키 복원→재마이그레이션 경로로 계속 동작(복원분도 다음 로드에서 청소)
- SPEC 9절 6단계·12절 갱신. 검증: 테스트 94건·빌드 통과
- ⚠ **push 보류** — 아이패드 실기기에서 v2 마이그레이션 정상 확인(07-19 이월 항목 겸) 후 push 권장. 배포되면 각 기기 두 번째 로드부터 구 키가 지워져 v1 롤백 보험이 소멸한다
- 다음: **Phase 4 E1 캔버스 쓰기 모드** (ROADMAP — 8월 3주 동결 전 마감)

---

## 📌 세션 (2026-07-31 밤) — 문헌 상세 모달 시각 개편 (v2.1.0) + 일괄 push

- **문헌 상세 오버레이 시각 개편** (v2.1.0 — 액션 위계 원칙 적용, 톤 유지):
  ① 표지 파스텔을 옅게 워시한 **히어로 밴드** — `color-mix` 그라디언트, 아래로 서피스에 페이드. 풀블리드는 `.detail`의 `--dp-x`/`--dp-t` 패딩 변수 공유로 계산
  ② 레벨 장수 칩 4개 → **단일 통계 밴드**(`.detail-stats`/`.dstat` — 숫자 위·레이블 아래·헤어라인 구분). 버튼으로 오독되던 affordance 제거, `.lv-chip` 폐기
  ③ 관리 액션 4개는 구분선 아래 조용한 아이콘+텍스트 버튼(`.dua-btn`, 인라인 SVG 1.5px 스트로크)으로 강등 — **문헌 삭제는 `margin-left:auto`로 우측 격리**. 학습 알약(순차 재생 primary·안키 모드)만 히어로에 유지
- 검증: 빌드(lint·tsc·vite) 통과, 헤드리스 크롬 실측 — 데스크톱·375px 모두 정상. ⚠ 헤드리스 크롬은 **최소 창폭 500px**이라 폰 미디어쿼리 직접 테스트 불가 → iframe(375px) 하네스로 우회
- **push·배포** — 2.1.0 push (Actions 배포 확인). 저녁 세션의 '2.0.0 미푸시' 표기는 **이 세션 시작 전 이미 해소**돼 있었음 (v2.0.1까지 13:36 배포 success — 검증 체크리스트도 그때 소화된 것으로 보임)

---

## 📌 세션 (2026-07-31 저녁) — **v2 전면 구현 완료 → 2.0.0**

ROADMAP Phase 3(S0~S4)을 당일 완주. 전 커밋 로컬 보관 — **푸시는 아래 검증 후**.

| 커밋 | 내용 |
|------|------|
| v1.20.1 (S0) | 베이킹 8문헌 카탈로그 등록 — 카드 id 고정, 교재 skipped 6건 순번 승계(시퀀스 1~79 완성) |
| v1.20.2 (S1a) | DocJSON v2 타입(editedAt·status·origin·source 확장) + v1→v3 마이그레이션 순수 로직 |
| v1.20.3 (S1b·c) | **비호환 전환 핵심** — 유저 공간(mundok-v3/docs) 단일 로드·편집 id 단일 경로(텍스트 델타 소멸)·ServerStore 삭제·컬렉션 선반(_collections.json)·C6 머지(editedAt 보존·removed 재도입 방지) |
| v1.20.4 (S1d) | 폐지 집행 — server.py·문독.command·src/data JSON 삭제(-8,398줄) + promote.mjs·C7 내보내기 |
| v1.20.5 (S2) | ReviewLog(append-only, fail_count 의미론 재현·compact)·Store 3계층 전담·Progress 마이그레이션 |
| v1.20.6 (S3) | 백업 v3(3계층 구조) + v2·구포맷 가져오기 호환(재마이그레이션 경유) |
| v1.20.7 (S4a) | 온보딩 스타터 슬라이드(C8) — 不治已病治未病 원클릭 담기 |
| **2.0.0** | README 저작·배포 절 재작성, SPEC as-is 전환+구현 노트(12절 — id 보존 등 편차 4건), 릴리스 |

- 검증: **테스트 94건·tsc·lint(카탈로그 81)·빌드 전부 통과** (매 커밋 확인). 구 `hanja-v2/*` 키 보존(≥1 마이너 후 제거)
- ⚠ **푸시 전 필수 검증** (사용자): ① 현 배포본(v1.20.0)에서 **백업 내보내기** ② `npm run dev` 로컬에서 ⤓ **가져오기로 그 백업 복원** → 자동 새로고침 → 마이그레이션 육안 확인 — 8문헌·학습기록·interp·선반 유지 (**localStorage는 origin별**이라 localhost엔 실데이터가 없음 — 백업 경유가 정식 절차) ③ 신규 사용자 시나리오(시크릿 창): 빈 서가→온보딩 스타터→문헌 받기 ④ 이상 없으면 push (일괄 배포)
- 잔여: 아이패드 실기기(셀 선택·마법사·카탈로그 — 07-19 이월), interp 나머지 문헌 적용(콘텐츠), 구 키 제거(2.1)

---

## 📌 세션 (2026-07-31 오후) — v2 아키텍처 확정, 계획 문서 3종 정비

- **v2 아키텍처 공리 확정** (사용자 결정): **저작 모드 폐지** — 문헌의 세계는 카탈로그/유저 공간 2곳, 유입 경로 2개(직접 생성·받기), 유저 공간 문헌은 전원 동등, 승격=내보내기→`catalog/` 커밋. 세부 결정 7건(id 체계·`editedAt` 머지·온보딩 스타터·server.py 삭제·ReviewLog·컬렉션 위치·`mundok-v3/` 접두사)까지 전부 확정 — `SPEC.md` 11절 결정 기록
- **SPEC.md 전면 재작성** — as-is 전수 조사 → **v2 to-be 명세**로 전환 (공리·도메인 모델·기능 명세·승격 파이프라인·마이그레이션·P1~P12 해소 매핑). 2.0.0 릴리스와 함께 as-is가 된다
- **ROADMAP.md 재편** — "8월 설계만·구현 개강 후" 당일 폐기(개강 후 가용 시간 없음): Phase 3 = **v2 전면 구현(8월 1~2주, S0~S4) → 2.0.0**, 사진 인제스트는 개강 후 Phase 6으로 이동 (D1은 server.py 폐지로 클라이언트 직접 호출 재설계 필요 — 열린 질문)
- v1.20.0(interp 1차)은 **푸시 완료 확인** — 아래 세션의 '미푸시' 표기는 해소. interp 나머지 문헌 적용·육안 확인은 콘텐츠 작업으로 계속
- **S0 완료 (v1.20.1)** — 베이킹 8문헌 카탈로그 등록. 카드 id(`c/w/s/p`+순번)는 JSON에 이미 저장돼 있어 **복사만으로 id 고정 충족**. 교재 skipped 6건(편작전→편작육불치 발췌·대의정성·식무구포·여담론·불치이병치미병·양성편)은 매니페스트 `no`를 `order`로 승계해 **교재 시퀀스 1~79 완성**, 교재 밖 2개(사기조신대론·상고천진론)는 order 없음. lint ERROR 0·테스트 76건 통과. 당초 우려한 '받기 중복 노출'은 기우 — `catalog.ts:104` **'내장됨' 가드**가 이미 처리. push는 v2 완성까지 보류(중간 상태 배포 무의미)
- **다음**: **S1** — Content 계층: DocJSON v2(`schemaVersion`·id 체계·`origin`·`editedAt`) · 편집 단일 경로(텍스트 델타 폐지) · 폐지 집행(ServerStore·server.py·문독.command·베이킹 glob) · 승격 파이프라인(C7+`promote.mjs`)

---

## 📌 세션 (2026-07-27~31) — 교재 카탈로그 배포·mode 화면 폐지 + interp 1차 (v1.17.1~v1.20.0)

### 배포된 것 (07-20 ~ 07-28)

| 버전 | 내용 |
|------|------|
| v1.17.1~4 (07-20) | 폴리시: 학습 화면 Space 힌트 정리(정답 보기 단독), 탭 제목·PWA 설치명 한글화(文讀→문독). 콘텐츠: 여담론 뒷부분 추가(精不足者/形不足者 대구·局方 온보 비판 단락) |
| v1.18.0~2 (07-27) | **문헌 진입 UX 통일** — 모든 문헌 클릭 = 상세 오버레이, `mode` 화면 폐지. 오버레이 safe center 정렬(v1.18.1), 주 모드를 순차 재생으로 — 버튼 순서·강조 및 `Enter`=순차/`⇧Enter`=안키(v1.18.2) |
| v1.19.0~2 (07-28) | **교재 신편의학한문 73문헌 카탈로그 배포** (sentence 레벨만 — 뜻·독음 0%, SPEC P10). 폰트 서브셋이 카탈로그 한자 누락하던 문제 수정(v1.19.1), lint가 미커밋 `src/interp.ts`를 참조해 배포 빌드가 깨지던 문제 수정(v1.19.2) |

### v1.20.0 (2026-07-31 — 푸시 완료)

- **해석 순서(interp) 편집·재생 1차** (v1.20.0) — 편집(文 메뉴 `해석 순서 편집`, 드래그 토글, 문법 편집과 상호 배타)·재생(카드 도구 ▶). 신규 `src/interp.ts`·`src/interp-play.ts`·`test/interp.test.ts` + 15개 파일. 정적 델타 `userdata.interp`(cardFront 키), 서버 `/api/save-interp`, lint에 청크 검증(범위·겹침). **▶는 `card.interp` 있는 sentence 카드에만 노출 — 미적용 문헌 UI 불변** (테스트 fail 0·빌드 통과 확인 후 커밋). 데이터는 불치이병치미병 일부만 — **나머지 문헌 적용·세부 마무리 잔여**

### 문서·스테이징 정리 (07-29 ~ 07-31)

- `SPEC.md` 작성(07-29 as-is 전수 조사)·편입 — 구조 문제 P1~P12, v2 방향(8절) 확정
- `IMPROVEMENTS.md` 삭제 — 전 항목 완료 (잔여 '문독.bat Windows 검증'은 폐기). 개선 항목 관리는 SPEC 7절로 일원화
- PROGRESS를 세션 로그 전용으로 축소 — 과거 로그는 `PROGRESS-archive.md` 분리, stale 참조 섹션(파일 구조·스키마·기능·단축키) 삭제
- `ROADMAP.md` 재작성·`RENEWAL.md` 삭제 — 계획 문서 일원화 (미착수 D·E는 ROADMAP Phase 3·4로 이식). **8월 우선순위 확정**(사용자): Phase 3·4 → 동결·베타 유지, v2는 8월 설계만·구현은 개강 후. **2.0.0 기준 변경**: 리뉴얼 완료 시 → v2 완성 시 (CLAUDE.md 예외 조항 폐기)
- `src/data/textbook/` 스테이징 정리 — JSON 사본 73개는 catalog와 전수 대조(동일 확인) 후 삭제, OCR PDF(123MB)는 `~/Documents/문독-원본PDF/` 이동(체크섬 검증). **`_manifest.json`(79문헌 출처 메타 — skipped 6건 판단 기록 포함 유일본)만 커밋 보존**, DocJSON 스키마 이전은 v2에서 (SPEC P9)
- `문독.bat` 삭제 — Windows 런처 폐기(사용자 결정), 관리자 저작은 macOS `문독.command` 단일 경로

### 다음 후보 (07-31 오후 세션에서 대체 — 상단·ROADMAP 참조)

---

## 📌 세션 (2026-07-19) — 콘텐츠 개방 3종: 셀 선택 저작·문헌 추가·카탈로그 (v1.15.0~v1.17.0)

사용자 논의로 설계 확정 후 순차 구현. **모두 커밋·배포 완료.**

| 커밋 | 내용 |
|------|------|
| v1.15.0 | **셀 기반 범위 선택 저작** — 카드 추가 텍스트 선택을 네이티브 선택(selectionchange·롱프레스)에서 글자 셀 포인터 스윕으로 전면 교체. 모드 진입 없는 디폴트 동작: 마우스·펜=즉시 긋기, 터치=220ms 홀드(떼면 한 글자) 또는 가로 우세 이동(세로는 스크롤 양보). 판정은 `select-gesture.ts` 순수 상태 기계(테스트 11건), 배선은 `cell-select.ts`. `#card-front` user-select:none + touch-callout 차단 — R7 선택 복제 트릭 제거 |
| v1.16.0 | **문헌 추가** — `hanja-v2/user-docs`(DocJSON 배열, `u1…` id, updatedAt) + 홈 '내 문헌' 선반·새 문헌 타일. 마법사: 제목·부제·표지색 → 본문 붙여넣기 → 자동 분할(줄바꿈 우선, 없으면 종결부호) 미리보기 → 일괄 생성. 사용자 문헌 카드 CRUD·문법은 델타 아닌 **id 기반 직접 수정**(LocalStore 분기). 관리자 모드는 `/api/create-doc`이 `src/data/*.json` 생성(curl 5케이스 검증). 상세 오버레이에 본문 추가·정보 수정·삭제 |
| v1.17.0 | **문헌 카탈로그** — `catalog/*.json` → 빌드 시 `dist/catalog/`(index.json 포함, build-catalog vite 플러그인) → Pages 동일 origin 배포. 홈 '문헌 받기' 모달: 받기·업데이트·설치됨 상태, SW 캐시는 ?v= 쿼리 버스팅. 설치본 = source 메타 붙은 user-doc, 업데이트는 카드 id 유지 통째 교체(학습기록 보존). lint가 카탈로그도 검사. 카탈로그는 이 시점 비어 있음(→ v1.19.0에서 73문헌 등록) |

- 검증: 테스트 69건(신규 20)·tsc·빌드·lint 통과, create-doc 실서버 curl 검증
- **설계 결정** (사용자): 셀 선택은 아이콘 진입 없이 디폴트 / 데스크톱도 셀 선택으로 통일 / 붙여넣기 마법사+단건 병행 / 베이킹 문헌 8개는 유지, **2.0에서 카탈로그로 완전 이전(굿노트형)**
- **서비스화 검토 결론** (사용자 질문): 네이티브 앱 기각(배포 속도 상실 > 실익 0) — PWA 유지. 동기화는 필요 시 `BackendStore`+BaaS(Supabase류)로, user-docs의 문헌 단위 updatedAt이 그 대비. 배포 시 홈 화면 설치(PWA) 유도 온보딩 권장
- ⚠ **잔여 확인**: 아이패드 실기기 — **셀 선택**(홀드·가로 긋기·펜슬), 새 문헌 마법사 터치, 카탈로그 모달. 데스크톱 육안 — 선택 하이라이트·버블 위치

---

> 2026-07-19 이전 세션 로그(리뉴얼 Phase 1~2, 반응형·터치, CSV→JSON 마이그레이션 등): **`PROGRESS-archive.md`**

---

## 📌 세션 (2026-07-18 밤) — 반응형·터치 재개: 5·6단계(C2·C3) + 폰 지원 (v1.11.2~v1.14.0)

**서비스화 방침에 따라 보류를 해제**하고 반응형·터치를 구현. 기기 범위 확정(사용자): 데스크톱=전부 / **아이패드=학습+현재 저작 모드 전부** / **폰=학습 플로우만**(홈→학습→결과·백업).

| 커밋 | 내용 |
|------|------|
| v1.11.2 | 상단 잘림 해소 — body·`#app`·`.modal-backdrop`을 safe center(auto 마진)로, 여백 `clamp(16~48px)`+safe-area, `min-h-dvh`, viewport-fit=cover |
| v1.12.0 | 터치 조작성 — `flipCard()` 통합(Space+카드 탭+"정답 보기" 버튼), 결과 "다시 시작" 실 버튼, 커스텀 `:hover` 17곳 `@media (hover:hover)` 게이팅, `.kb-only`(kbd·숫자 배지 터치 숨김), 터치 타깃 44px(`pointer:coarse`), tap-highlight 제거 |
| v1.13.0 | 저작 터치 — 카드 추가 버블 mouseup→**selectionchange**(롱프레스 대응, coarse는 선택 아래 배치), 문법 편집 mouse→**pointer events**(펜슬 포함, elementFromPoint+`touch-action:none`) |
| v1.14.0 | 폰 브레이크포인트(≤480px) — 홈 헤드 wrap·워드마크 clamp·히어로 버튼 전폭, mode 타일 1열, backBtn truncate, 안키 숫자 접두 kb-only, 결과·모달 여백 축소 |

- 검증: 테스트 49건·tsc·빌드·lint 전 단계 통과. IMPROVEMENTS 🔴4 해소
- **배포**: 이 세션 푸시로 리뉴얼 전체(v1.2.0~v1.14.0)가 GitHub Pages에 첫 공개됨. **폰 실기기 확인 완료** (2026-07-18, 사용자)
- ⚠ **잔여 확인**: 아이패드 저작 모드 실기기 — 롱프레스 선택→카드 추가 버블(선택 텍스트가 모달로 넘어가는지), 문법 편집 드래그(손가락·펜슬) — 다음 세션 첫 작업으로
- 비범위로 남김: 스와이프 제스처, 폰 저작 최적화

---

## 📌 세션 (2026-07-18) — 글자 셀 렌더링 + 문법 UI 개선 (v1.8.0~v1.11.0)

한 세션에서 **① 학습 카드 본문 구조 개선(글자 셀)** + **② 문법 UI 3차 반복 개선** 완료. UI 디테일 다듬기(사용자 피드백 기반) 흐름의 일부.

### ① 글자 셀 렌더링 통일 (v1.8.0~v1.8.1) — 명세·결정 기록: `design/char-cell.md` (R1~R9)

- **문제**: 음·드릴 밑줄·문법 표시가 문자열에 각기 다른 방식으로 덧칠 → 전체 길이 일치 시에만 글자별 음 / 뒤집을 때 배치 흔들림 / 드릴 안팎 음 위치 상이 / 드래그에 음 섞임
- **`src/reading-align.ts` 신설**: 한자에만 1:1 독음 정렬(공백·부호·현토 통과), 실패 시 null — 렌더러·lint 단일 출처 (lint가 `.ts` 직접 import — Node 22.18+ 기본 type stripping)
- **`render.ts` 통일**: 구 렌더러 3종(ruby·inline-block·상태기계) 폐지 → `renderFront()` 글자 셀(`.cc`) 단일 경로. 음(`.cc-rd`)·SVO 레이블(`.cc-svo`)은 absolute → 앞뒷면 줄바꿈·가로 배치 동일, 음 토글 시 밑줄 부동. 편집 그리드도 같은 표기(`.cc-rd-e`)
- char 카드 하단 예약 `.char-hold`(R6), 드래그 선택은 범위 복제 후 음 노드 제거(R7), lint 독음 정렬 WARN — 한자 2자 이상만, 현 데이터 0건(R5)
- v1.8.1: 음-밑줄 간격 8px + 음 표시 중 `rd-on` 줄 간격 `calc(1em+46px)` 확장 (피드백)

### ② 문법 UI (v1.9.0→v1.11.0 반복, 최종 상태)

- 구 `文法|보기|편집` 세그먼트 폐지 → **카드 도구의 文 아이콘 클릭 시 확장 메뉴**(`.gram-menu`): `문법 표시`(G 키캡)·`문법 편집` 두 토글 항목, 활성 시 파란 표시. 아이콘도 켜짐 상태면 파란 활성
- **문법은 카드 단위 수동 켜기**: `resetGrammarView()`(state.ts)가 카드가 바뀌는 모든 시점(순차 이동·안키 rate·드릴다운 진입/복귀·학습 시작·홈)에 자동 OFF. 같은 카드 안 Space 뒤집기는 유지
- 단축키 **G**(문장 카드, 도움말 등재). 메뉴 닫힘: 항목 선택·바깥 클릭·Esc·카드 이동
- 중간 산물이던 하단 文法 필(v1.9.0)은 v1.11.0에서 제거됨

### 검증·잔여

- 테스트 49건(신규 15) 통과, tsc·빌드·lint 통과. 커밋 c57b72a→7ba53d1 (푸시 안 함)
- ⚠ **육안 확인 잔여**: char-cell.md 4절 7항목(뒤집기 배치 불변·드릴 안팎 음 높이·드래그 음 미포함 등) + 文 메뉴 인터랙션
- 잔여 재량: `.char-hold` 150px·음 줄 간격 46px 등 수치 미세 조정은 육안 피드백 후
- ~~다음 작업: UI 디테일 다듬기 계속, 5·6단계(반응형·터치)·Phase 3 보류~~ → **5·6단계는 2026-07-18 밤 보류 해제·완료** (위 최신 섹션). Phase 3(사진 인제스트)만 계속 보류

---

## 📌 세션 (2026-07-19) — Phase 2 4단계 완료 (서가 홈 A1~A6, v1.5.0~v1.6.0)

`RENEWAL.md` 5절 4단계 구현 완료 — 커밋 `a386988`(서가 홈, v1.5.0) + `bc9e42d`(그룹 편집, v1.6.0):

- **스키마**: `src/data/_groups.json` 신설(shelves+refs, 하드코딩 `DOC_GROUPS` 흡수 — 미결 ① 구현), `DocJSON.color?` + 팔레트 8색 자동 배정, 로더·lint·서브셋 스크립트 `_*.json` 제외, lint에 그룹 참조·color 형식 ERROR (+테스트 3건 = 34건)
- **서가 홈**: 워드마크(해서)+streak 필(A5), 이어서 학습 히어로(A3 — `hanja-v2/last-session`, seq 인덱스 복원), 선반=그룹+접기(localStorage), 표지(제첨·키보드 배지 A6·장수·최근 칩, 파스텔 A2), 참고문헌 겹침 무더기 → **문헌 상세 오버레이**(A4 — 모드 시작은 mode 화면 생략하고 바로 level로, 참고문헌 숫자키 재부여, Esc/바깥 닫기)
- **그룹 편집(A1, 저작 모드)**: 홈 첫 선반 헤더 "그룹 편집" → 모달(이름·추가·삭제·↑↓ 순서·선반 간 이동). `server.py /api/save-groups`가 검증 후 원자적 기록 — 실서버 curl 3케이스 검증(정상·깨진 참조 거부·형식 오류 거부). refs는 보존
- ⚠ **서버 재시작 필요**: 그룹 편집 API는 새 server.py — `문독.command` 재실행 후 사용
### 방향 결정 (2026-07-19, 사용자)

- **5·6단계(반응형·터치·3해상도 검증) 보류** — 모바일 확장은 후순위 연기 (IMPROVEMENTS 🔴4도 계속 보류)
- **Phase 3(사진 인제스트) 보류**
- **다음 작업: UI 디테일 다듬기** — 리뉴얼 골격(1~4단계, v1.2.0~v1.6.0)은 완성. 실사용하며 눈에 걸리는 디테일(간격·정렬·색·타이포·모션·마이크로 인터랙션)을 **사용자 피드백 기반으로** 잡는다. 사용자가 화면 보면서 지적 → 수정 반복
- 디테일 정리가 끝나면 보류 항목(5·6단계 / Phase 3) 재개 여부 재논의

---

## 📌 세션 (2026-07-18 심야) — Phase 2 3단계 완료 (학습·결과·모달 리디자인, v1.4.0)

`RENEWAL.md` 5절 3단계 구현 완료·커밋(`df80642`):

- **학습 화면**: 본문 스케일 목업 이식(`.sentence-body` clamp 20~27px·행간 2.6 / `.paragraph-body` / char `.bigchar` 해서 clamp 84~124px), 드릴다운 `.drill`(액센트 호버), 문법 S/V/O 색 토큰화, 文法 토글 `.seg`, 카드 도구 `.icon-btn`, 안키 난이도 `.rate-btn` 3종, 카드 뒷면 `.card-back`/`.reading-line`/`.meaning`
- **mode·level**: 선택 카드 `.tile`(라운드 18px·스프링 호버) + 단축키 힌트 키캡화
- **결과 화면**: `.stat` 통계 타일 3종 + `.table-card` 오답표 + 키캡 푸터 (목업 화면 4 구조)
- **모달 5종**(추가·수정·연결카드·온보딩·단축키): `.modal-backdrop`(블러) + `.modal-surface`(라운드 24·pop), 버튼 `btn-primary`/`btn-ghost` 통일
- 검증: 빌드·테스트 31/31, 신규 클래스 17종 번들 확인. ⚠ 브라우저 육안 확인 필요(학습·결과·모달 전 화면)
- 남은 C1: 서가 홈(4단계). 백업 FAB 팝업·온보딩 내부 슬라이드 스타일은 잔여 stone 톤 — 4~5단계에서 마무리

**다음**: 4단계 — 서가 홈 (`_groups.json` 스키마 + A1~A6 + 문헌 상세 오버레이)

---

## 📌 세션 (2026-07-18 밤) — Phase 2 2단계 완료 (토큰·공용 컴포넌트, v1.3.0)

- **SW 캐시 자동 무효화** (v1.2.1→v1.2.2): 서체 전환이 화면에 안 보이던 원인 = SW가 구 셸 캐싱 → 캐시 버전 인상으로 해결 후, `sw-cache-version` vite 플러그인으로 **빌드 시 package.json 버전 자동 주입** — 릴리스마다 셸 캐시 자동 무효화, 수동 인상 규칙 폐지 (폰트 캐시는 수동 유지)
- **2단계 (v1.3.0)**: `src/style.css`에 디자인 토큰 `:root`(색·그림자·스프링 이징·S/V/O) + 공용 컴포넌트 9종(`.btn-primary/.btn-ghost/.nav-btn/.btn-back/.icon-btn/.card-surface/.progress-track/.progress-fill/.kbd`) — 목업 `final.html`에서 이식. 앱 크롬 #F4F4F6 전환(index.html 첫 페인트 리터럴 + manifest 동기화). 순차·안키 학습 화면(진행바·카드 서피스·이전/다음·Space 키캡)·뒤로/홈 버튼·단축키 도움말에 적용
- 검증: 빌드·테스트 31/31, 번들 CSS 클래스 9종 생성 확인. 안키 난이도 버튼·모드/레벨/결과 화면은 3단계에서 리디자인
- **다음**: 3단계 — 학습 화면(순차·안키·결과)·모달 리디자인 (`RENEWAL.md` 5절)

---

## 📌 세션 (2026-07-18 저녁) — Phase 2 1단계 완료 (서체·에셋 기반, v1.2.0)

`RENEWAL.md` 5절 1단계(B1~B3) 구현 완료·커밋(`03d966d`):

- **B1 폰트 스택 전환**: 명조(Noto Serif) 전면 제거 → 고딕 사슬(`Pretendard→Noto Sans KR→TC→SC`). 해서 `.kai` 클래스 신설 — 홈 워드마크·홈 카드 장식 대형 한자·char 카드 대형 한자 3곳만 적용(16px 미만·학습 본문 금지 준수). sw.js 폰트 캐시에 jsdelivr 추가(Pretendard 오프라인)
- **B2 WenKai 서브셋 self-host**: `npm run font:subset`(`scripts/subset-font.mjs`) — 콘텐츠 한자 610자만 서브셋 → `public/fonts/wenkai-tc-sub.woff2` **180KB**(원본 14.6MB). 원본 TTF는 `.fontcache/`(gitignore) 캐시, 산출물·글자 목록·OFL 라이선스는 커밋. 새 문헌 추가로 글자가 늘면 재실행
- **B3 커버리지 lint**: `lint-data.mjs`가 콘텐츠 한자 ⊄ 서브셋이면 WARN(재생성 안내). 단일 출처 `collectHanChars()`를 서브셋 스크립트와 공유
- **검증**: 빌드(lint ERROR 0·폰트 WARN 0 + tsc + vite) 통과, 테스트 31/31, dist/fonts 산출 확인. ⚠ 브라우저 육안 확인은 사용자 몫(서버 기동 후 홈·char 카드 서체 확인)

**다음**: 2단계 — 공용 컴포넌트·토큰 CSS (`design/tokens.md` 기준 버튼·카드·프로그레스·kbd)

---

## 📌 세션 (2026-07-18) — 리뉴얼 미결 사항 처리

`RENEWAL.md` 4절 미결 5건 처리 (문서만 변경, v1.1.0 유지):

- **① 그룹 스키마** → 별도 **`src/data/_groups.json`** 확정 — 선반 그룹(`shelves`) + 참고문헌 관계(`refs`, 하드코딩 `DOC_GROUPS` 흡수)를 한 파일에 정의. 그룹 편집 UI·lint 참조 검사 용이, 복수 소속 표현 가능
- **④ WenKai 오프라인** → **사전 서브셋 + 커밋** 확정 — `subset-font` npm(devDep)으로 콘텐츠 한자+제목만 woff2 생성해 `public/fonts/`에 커밋, B3 커버리지 lint가 새 글자 감시. CI 변경 불필요
- **⑤ 버전** → 리뉴얼 완료 릴리스에 **2.0.0 부여** 확정 — `CLAUDE.md` 버전 규칙에 예외 명시 완료
- **②(vision API 키)·③(PPT 사진·써머리 PDF 샘플)**: 미확보 확인 — Phase 3 착수(8월 초) 전 준비, 미결로 잔존 (Phase 2 블로커 아님)

**다음 세션**: Phase 2 구현 착수 — `RENEWAL.md` 5절 1단계(B1~B3 서체·에셋 기반)부터. **블로커 없음.**

---

## 📌 세션 (2026-07-17) — 2학기 리뉴얼 설계 완료

2학기 개강 전 대규모 기능 추가 + UI/UX 리뉴얼의 **설계 세션 완료** (코드 변경 없음 — 문서·목업만, v1.1.0 유지).

- **단계 계획**: `ROADMAP.md` — 사용자 인터뷰 기반 Phase 1~5 (개강 9월 초 역산, 8월 중순 기능 동결·실사용 베타). Phase 1(디자인) 완료 기록 포함
- **구현 요구사항 전체**: `RENEWAL.md` — 기능 17개(서가 홈·서체 엔지니어링·리디자인·사진 인제스트·쓰기 모드), 스키마 변경(전부 하위호환), 작업 순서, 미결 5건
- **디자인 확정**: GoodNotes 라이브러리 콘셉트 — 수치는 `design/tokens.md`, 시각 기준은 `design/mockups/final.html`(브라우저로 열면 서가 홈·문헌 상세 오버레이 인터랙션까지 확인 가능)
- **다음 세션**: Phase 2 구현 착수 — `RENEWAL.md` 5절 순서(① 서체·에셋 기반 B1~B3 → ② 공용 컴포넌트 → ③ 학습 화면 → ④ 서가 홈 → ⑤ 반응형). ⚠ 서가 홈(④) 착수 전 미결 ①(그룹 스키마 위치) 결정 필요

---

## 📌 세션 (2026-07-08)

`IMPROVEMENTS.md` 🟡 중간 항목 5개(5·6·8·9·10) 완료·push 완료. 🔴 높음 4번(모바일 레이아웃)은 사용자 지시로 보류.
🟢 낮음 **원본 PDF 분리** 완료 — PDF 7개를 `~/Documents/문독-원본PDF/`로 이동(체크섬 검증), `git filter-repo`로 전체 히스토리에서 제거 후 force-push(전 커밋 해시 변경, 저장소 8.58MiB→433KiB). ⚠ 다른 기기에 기존 클론이 있다면 새로 clone 필요.

🟢 낮음 나머지 4건도 완료: ① server.py 미사용 `time` import 제거 ② 홈 화면 버전 문자열 `package.json` 주입 + 패키지명 `mundok` 정리 ③ 드릴다운/문법 인덱스 코드포인트 통일(`findDrillSpans` 코드포인트화, 벽자 회귀 테스트 추가, lint WARN 전후 동일) ④ `DocJSON.order` 필드로 홈 화면 순서 지정 지원(미설정 시 기존 파일명 순 유지). 🟢에서 남은 건 `문독.bat` Windows 검증(기기 필요)뿐 — **다음 세션 후보**: 보류 중인 🔴 4번(모바일 레이아웃).

| 순서 | 항목 | 내용 |
|------|------|------|
| 1 | ⑤+⑥ 저작 API id 기반 전환 + 404 | `server.py` edit/delete/save-grammar가 텍스트 대신 `card.id`로 카드 탐색(중복 텍스트 전체삭제 위험 제거). 대상 미발견 시 404 반환. `add-card`가 신규/기존 id를 응답에 포함해 방금 추가한 카드를 같은 세션에서 바로 수정/삭제해도 깨지지 않음. curl로 실서버 기동해 add/edit/delete/grammar id 매칭·404 케이스 검증 |
| 2 | ⑧ "최근 학습" 순차 모드 반영 | `state.ts`에 `touchLastStudied()` 분리(fail_count 안 건드리고 `_ts`만 갱신), 순차 모드 Space 플립(뒷면 확인) 시점에 호출. 안키 전용이던 최근 학습일 표시가 순차 모드도 반영 |
| 3 | ⑩ 자동 테스트 + CI | `node:test`+`--experimental-strip-types`(Node 22 내장, 의존성 0)로 `test/` 27건. 안키 큐 재배치(`anki-core.ts`)·사용자 델타 병합(`docs-merge.ts`)·lint 규칙·드릴다운 매칭을 순수 함수로 분리해 테스트 가능하게 함. `.github/workflows/ci.yml`(PR 트리거) 신설 + `deploy.yml`에도 테스트 스텝 추가. 부수 발견: `lint-data.mjs`의 CLI 가드가 한글 경로(`문독`)에서 URL percent-encoding 불일치로 무력화되던 버그 수정 |
| 4 | ⑨ 드릴다운 매칭 통합 + render.ts 분리 | `buildDrillMap`/`tokenizeHighlights`/`annotatedFront` 3중 복제를 `src/drill-match.ts`의 `findDrillSpans()` 단일 출처로 통합. render.ts(958줄)에서 `onboarding.ts`·`shortcut-help.ts`·`result-screen.ts`·`render-shared.ts` 분리 → 585줄. lint 드릴다운 WARN 건수 리팩터 전후 동일함으로 매칭 결과 불변 확인 |

세부 근거·검증 내역은 `IMPROVEMENTS.md`(삭제됨 — git 히스토리) 해당 항목 참고. **다음 세션 후보**: 🟢 낮음 항목들(코드포인트/UTF-16 인덱스 혼용 통일, 원본 PDF 분리, 버전 문자열 하드코딩 등) 또는 보류 중인 🔴 4번(모바일 레이아웃).

---

## 📌 세션 (2026-07-07)

`IMPROVEMENTS.md` 추천 착수 순서(① Tailwind → ③ 백업 → ② PWA → ⑦ streak) 4개 항목 완료·커밋 완료(`3eaf81a`).

| 항목 | 내용 |
|------|------|
| ① Tailwind 빌드 전환 | Play CDN 제거 → Tailwind v4 + `@tailwindcss/vite`. `src/style.css` 추가, `vite.config.ts`→`.mts`(ESM 플러그인 로드). 온보딩 슬라이드의 분리형 동적 클래스(`bg-${bg}`) 1건 수정. CSS 24.77KB(gzip 5.5KB) |
| ③ 백업 범위 확장 | `exportUserData`/`importUserData`가 `hanja-v2/` 접두사 전체 키(안키 오답·streak·최근학습일 포함) 대상으로 확장. 신 포맷(`{version:2,keys}`) + 구 포맷(userdata 단일 객체) 하위호환. grammar 항목까지 구조 검증 강화 |
| ② PWA 전환 | `public/manifest.json`(아이콘은 `icon.svg`를 `sips`로 192/512px 래스터화) + `public/sw.js`(앱 셸 stale-while-revalidate, 프로덕션 빌드에서만 등록) + `storage.persist()`. Google Fonts self-host는 레포 용량 문제로 SW 런타임 캐싱으로 대체 |
| ⑦ streak UTC→로컬 | `state.ts`에 `localDateStr()` 추가, `todayStr()`/어제 계산 양쪽에 적용. KST 08:30(UTC 전날 23:30) 경계 재현 테스트로 수정 확인 |

세부 근거·검증 내역은 `IMPROVEMENTS.md`(삭제됨 — git 히스토리) 해당 항목 참고.

---

## 📌 세션 (2026-06-30)

**배포 상태**: `refactor/csv-to-json` → `main` fast-forward 병합 후 push, **GitHub Pages 배포 완료**(2026-07-01, CI `33b203c` build+deploy 성공, 라이브 HTTP 200). 라이브: https://river11456.github.io/mundok/
병합 브랜치 `refactor/csv-to-json`은 정리(삭제)됨.

이 세션에서 마이그레이션 위에 8개 커밋 추가 배포: 여담론 문법 복구 · 양성편 s21 드릴다운 · 홈 키보드 수정 · 연결카드 일괄수정 프롬프트 · p6 정본 일치 · streak 부작용 분리 · 안키 큐 반영 · 포트 동적 탐색.

### 오늘 완료 — CSV → 문헌별 JSON 마이그레이션 (Phase 1~5 + 정리)

콘텐츠 저장을 `CSV + userdata.json` 델타 구조에서 **`src/data/*.json` 단일 진실**로 전환.
"텍스트 = 식별자" 구조 문제(드릴다운 추측·텍스트 수정 시 연결 끊김·진실 분열)를 해소.
세부 Phase 기록은 맨 아래 **🔴 데이터 아키텍처 개선** 섹션 참고.

| 커밋 | 내용 |
|------|------|
| `8885490` | Phase 1~3: CSV→JSON 무손실 변환 + 로더 교체 + server.py JSON 직접 편집 |
| `1fc37c0` | Phase 4a: 안키 학습기록 id 기반화 |
| `6d66d55` | Phase 5: 콘텐츠 무결성 빌드 lint |
| `69fdf35` | 정리: 구 CSV·userdata.json·죽은코드(`csv.ts`)·`papaparse` 제거 |

- **검증**: 변환 무손실(946장·문법19건), 서버 CRUD 통합 11/11, 안키 로직 7/7, 빌드·lint ERROR 0
- **부수 성과**: 여담론 고아 문법 1건 발견·격리 / edit 시 문법주석 유지 / 런타임 의존성 0개

### 남은 작업 (다음 세션 후보)

- [x] **배포** ✅ (2026-07-01) — `refactor/csv-to-json`→`main` fast-forward 병합 후 push, CI 빌드·배포 성공, 라이브 HTTP 200 확인
- [x] 여담론 고아 문법 1건 복구 ✅ (s21 `助` V 주석. 인덱스 5–6은 `充足` 텍스트에서도 그대로 유효 — `足`이 주석 범위 뒤라 보정 불필요)
- [x] 양성편 s21 드릴다운 보강 ✅ (`內外百病`·`自然不生` word 카드 추가 → s21 매칭 2건). s7·s8은 순수 숫자·시간 나열이라 보류(WARN 2건 잔존, 무해)
  - 근거: 전체 197개 부모카드 중 0건은 이 3개뿐 = 양성편 구조 문제 아닌 국소 저작공백. s7·s8 숫자 드릴다운은 교육 가치 낮아 의도적 보류
- [~] Phase 4b: LocalStore 편집 델타 id화 (보류 — 효용 낮음)
- [~] Phase 5a: 드릴다운 명시 링크 (보류 — 모호성 1건뿐, 자동매칭 충분)
- [x] 포트 동적 탐색 ✅ (server.py: 19234 우선 → 사용 중이면 OS 빈 포트 폴백 + `.runport` 기록. 런처: 무차별 kill 제거, 이미 떠 있으면 재사용)

---

## 데이터 아키텍처 전환 기록 (2026-06-30 — 완료)

### 🔴 데이터 아키텍처 개선 — CSV → 문헌별 JSON 전환 (완료)

#### 결정 (2026-06-30)

데이터 포맷을 **CSV → 문헌별 JSON**으로 통합한다. CSV는 평면 표만 표현 가능해, 한자 학습 데이터의 실제 형태(트리·그래프: 카드 간 드릴다운 관계, 카드 내 문법주석 배열)를 담지 못한다. 그 결과 표현 불가한 것들이 전부 `userdata.json` + 코드 휴리스틱으로 새어나가 **진실이 분열**됐다.

**실측 근거** — 이미 201개 콘텐츠가 CSV 바깥(`userdata.json`)에 산다:

| 종류 | 수 | 의미 |
|------|----|----|
| additions | 149 | CSV에 아예 없는 카드 (식무구포 59·양성편 68 → 사실상 통째로 JSON에 있음) |
| edits | 18 | CSV를 덮어쓴 것 |
| deletions | 14 | CSV에 있지만 안 보이는 것 |
| grammar | 20 | CSV가 담지 못하는 문법 주석 |

#### 문제 진단 — "텍스트 = 식별자"

안정적 카드 ID가 없어 모든 연결이 한자 텍스트 문자열을 키(PK)로 쓴다.

| 연결 | 키 | 위치 | 증상 |
|------|----|----|----|
| 수정 | `origText` | `docs.ts:83` | 텍스트 수정 시 연결 끊김 |
| 삭제 | `text` | `docs.ts:74` | 중복 텍스트는 양쪽 적용 |
| 문법 | `cardFront`(문장 전체) | `grammar.ts:11` | 문장 한 글자 수정 → 주석 통째 소실 |
| 드릴다운 | `front.includes(card.front)` | `render.ts:81` | 저장된 관계가 아닌 렌더링 시점의 추측 |
| 안키 | 카드 **개수** 일치 | `state.ts:54` | 카드 1장 추가/삭제 → 학습기록 리셋 |

#### 확정 스키마 (`src/data/<문헌>.json`)

```jsonc
{
  "id": "편작육불치",          // = 파일명 (기존 docId·localStorage 키 호환 위해 한글 유지)
  "title": "扁鵲六不治",
  "sub": "편작의 6가지...",
  "levels": {
    "char": [ { "id": "c1", "text": "驕", "reading": "교만할 교", "meaning": "", "note": "" } ],
    "sentence": [
      { "id": "s1", "text": "驕恣不論於理 一不治也", "reading": "...", "meaning": "...", "note": "",
        "grammar": [{ "type": "S", "start": 0, "end": 2 }],   // 카드 안으로 내장
        "drill":   [] }                                        // 명시 링크 (비면 자동매칭)
    ]
  }
}
```

- 카드 id: 문헌 내 `c1/w1/s1/p1` 순번, 한 번 부여하면 텍스트 무관 영구 불변.
- `DOC_GROUPS`(참고문헌 그룹)는 Phase 1에선 유지, 후속에 `parent` 필드로 흡수 검토.

#### 실행 단계 (작은 단위, 각 단계 독립 검증)

- [x] **Phase 1 — 무손실 변환** ✅ (2026-06-30)
  - `types.ts`에 `DocJSON`/`CardJSON` 스키마 추가 완료 (기존 타입 유지)
  - 변환 스크립트 `scripts/migrate-to-json.mjs` — PapaParse + applyUserData 재사용
  - **결과**: `src/data/*.json` 8개 생성. 카드 946장·문법 19건 **무손실 검증 통과**(왕복 동등성 100%)
  - CSV·userdata.json은 그대로 보존 (Phase 2에서 로더 교체 후 정리)
  - ⚠ **발견**: 여담론 문법 1건이 이미 고아(텍스트 `充→充足` 수정으로 cardFront 불일치) — 현재 앱에서도 표시 안 되는 죽은 데이터라 변환에서 제외. *이게 바로 "텍스트=식별자" 버그의 실제 사례.* → 복구는 아래 별도 항목
- [x] **Phase 2 — 로더 교체** ✅ (2026-06-30)
  - `docs.ts`가 `import.meta.glob('./data/*.json')`로 JSON 로드, 베이스 카드에 안정 id 부여
  - 베이킹 델타 계층(`bakedUserData` import) 제거, CSV import 제거, 카드 내장 grammar → `collectGrammar()`로 펼침
  - 사용자 localStorage 델타 적용 경로 유지 (id 전환은 Phase 4)
  - **검증**: `npm run build` 통과(타입 OK), 로더 출력 946장·19건 = Phase 1 canonical 일치, 카드 id 문헌 내 유니크, 홈 순서 보존
  - ⚠ **부수효과**: 서버 모드(관리자) 저작이 일시 무력화 — `docs.ts`가 `userdata.json`을 안 읽으므로 `server.py` 편집이 화면에 반영 안 됨. **Phase 3에서 복구.** 일반 사용자(정적)는 정상.
- [x] **Phase 3 — server.py JSON 직접 편집** (단일 진실) ✅ (2026-06-30)
  - `server.py`의 add/edit/delete/grammar API가 `src/data/<문헌>.json`을 직접 수정 (id 자동채번 `next_id`, grammar는 sentence 카드 내장)
  - 구 `userdata.json` 델타 계층 폐기, `/userdata.json` GET 핸들러 제거
  - **검증**: 실서버 통합 테스트 11/11 통과(add→c38 채번·edit·save-grammar·delete→복귀), JSON 출력 포맷 원본과 100% 일치(diff 깔끔), 원본 자동 복원
  - 서버 모드 저작 복구 + *"편집 = JSON = 화면 = git diff"* 일치 달성
  - ✨ **부수 개선**: 카드 edit 시 grammar가 카드 내장이라 텍스트를 바꿔도 주석이 끊기지 않음 (여담론 같은 고아화 방지)
- [x] **Phase 4a — 안키 기록 id 기반** ✅ (2026-06-30)
  - `state.ts`: `개수 일치`(`saved.length===cards.length`) → 카드 id별 `fail_count` 맵(`.../fails` 키)
  - 구 포맷(Card[] 배열) → front 매칭으로 1회 자동 마이그레이션(`migrateOldAnki`), 구 키 정리
  - `resetAnki()` 추가, `events.ts` hardReset 교체
  - **검증**: 빌드 통과, 안키 로직 시뮬레이션 7/7 — 카드 추가/삭제에도 학습기록 유지·구 기록 보존·초기화 정상
- [~] **Phase 4b — LocalStore 편집 델타 id 기반** (보류)
  - 정적 사용자 edit/delete를 텍스트 대신 카드 id로 참조 (`docs.ts applyUserData`, `events.ts`, `storage/local.ts`)
  - **보류 사유**: 정적 사용자 편집은 소규모이고 관리자 편집은 이미 JSON 직접(Phase 3). 호환 마이그레이션(text→id) 비용 대비 효용이 낮음. 정적 편집이 실사용에서 늘면 재개.
- [x] **Phase 5 — 빌드 lint(구 ⑤) ✅ + 드릴다운 명시 링크(구 ②) 판단** (2026-06-30)
  - `scripts/lint-data.mjs` 작성, `package.json` build 에 연결 (ERROR 시 빌드 차단, WARN 통과)
    - ERROR: 카드 id 중복 / drill 링크 깨짐 / 문법 인덱스 범위 이상
    - WARN: 같은 레벨 중복 텍스트 / sentence·paragraph 드릴다운 0건
  - **현재 데이터**: ERROR 0건, WARN 4건 (양성편 `百刻` 중복 1, 드릴 0건 3건 = 양성편 s7·s8·s21)
  - **5a(명시 링크) 보류 — 데이터 근거**: 모호성(중복 텍스트)이 1건뿐이고 같은 뜻이라 자동 substring 매칭으로 충분. `drill` 필드는 스키마에 옵션 유지(미래 모호성 대비, lint 가 무결성 검사).

#### 남은 정리 작업

- [x] **데이터 정리** ✅ (2026-06-30) — lint 발견 항목
  - 양성편 `百刻` 중복 w10 제거 (w6 완전판 유지)
  - 여담론 `"故로 以菜助其充足은…"` 고아 문법 → **복구 완료** ✅ (s21에 `助` V 주석 `{start:5,end:6}` 재부여. 원본 인덱스가 `充足` 텍스트에서도 유효해 보정 불필요. lint·빌드 통과)
  - 양성편 s7·s8·s21 드릴다운 0건(숫자/시간 표현) → 데이터 완성도 이슈, 필수 아님(미해결)
- [x] **구 파일 정리** ✅ (2026-06-30) — `src/data/*.csv`·`userdata.json` 제거, 죽은 코드 `csv.ts`·일회성 `migrate-to-json.mjs`·미사용 `papaparse` 의존성 제거, README/PROGRESS JSON 기준 갱신 (git 히스토리에 남아 복원 가능)

### 🟡 기타

- [x] **포트 동적 탐색** ✅ (2026-06-30) — `server.py`가 19234 우선 시도, 사용 중이면 `OSError` 잡아 OS 빈 포트(`port 0`)로 폴백하고 실제 포트를 `.runport`에 기록(종료 시 삭제). 런처(`문독.command`/`문독.bat`)는 무차별 `kill -9` 제거 → 이미 우리 서버가 떠 있으면 그 포트로 브라우저만 열고, 아니면 `.runport`를 읽어 실제 포트로 접속. (⚠ `문독.bat`은 macOS에서 미검증)
