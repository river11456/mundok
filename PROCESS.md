# 작업·버전·배포 관리

문독은 **할 일 등록 → 수정 → 검토 → 배포 확인 → 완료 기록** 순서로 관리한다.
사용자는 대화로 요청할 수 있고, 작업하는 사람이나 AI가 GitHub 기록을 갱신한다.
이 문서는 종전 `CLAUDE.md`의 “모든 코드·데이터 커밋마다 버전 증가” 규칙을 대체한다.

## 1. 작업 목록과 완료 기준

- [GitHub Issues](https://github.com/river11456/mundok/issues)가 공식 작업 목록이다. 기존 이슈를 먼저 확인해 중복 등록하지 않는다.
- 아이디어는 **검토 후보**로 등록한다. 진행하기로 정한 작업에는 문제, 범위, 완료 조건, 검증 방법을 적는다.
- 상태는 이슈 본문의 `상태`에 기록한다: **대기 → 착수 준비 → 진행 → 검토 → 배포 확인 → 완료**. 후보는 **검토 후보**, 중단한 작업은 이유와 함께 **보류**로 표시한다.
- 범위와 완료 조건이 정해지면 착수 준비, 실제 수정에 들어가면 진행이다. 주 작업은 한 번에 1개, 긴급 수정은 별도 1개를 기본으로 한다.
- 배포가 필요한 작업은 운영 확인과 출시 기록까지 남긴 뒤 이슈를 수동으로 닫는다. 문서·테스트·관리 도구만 바뀐 작업은 병합과 검증 기록으로 완료한다.

## 2. 브랜치와 PR

1. 최신 `main`에서 `codex/<이슈번호>-<짧은설명>` 브랜치를 만든다.
2. 이슈 범위 안에서 수정하고 필요한 테스트와 문서를 함께 갱신한다. 이미 허용된 일반 수정·검증은 매번 재확인하지 않고 진행한다. 범위가 크게 바뀌면 먼저 결정과 이유를 기록한다.
3. PR에 `Related #번호`로 이슈를 연결한다. 배포 전에 닫히지 않도록 `Closes`나 `Fixes` 같은 자동 종료 표현은 쓰지 않는다.
4. 최신 `main`을 반영한 PR의 필수 CI가 통과해야 한다. 작성과 분리된 리뷰 단계에서 변경 내용, 완료 조건, 버전 종류, 데이터 영향을 확인하고 근거를 남긴다.
5. 저장소 소유자가 출시 가능 여부를 판단해 squash merge한다. 1인 운영에서는 다른 사람의 GitHub 승인을 필수로 걸지 않되, CI와 별도 리뷰는 생략하지 않는다.

`main` 직접 push는 사용하지 않는다. 커밋 제목은 변경 이유를 짧게 쓰고, 필요한 경우 `Tested:`, `Not-tested:`, `Constraint:` 등으로 검증과 제약을 남긴다. 긴급 수정도 작은 PR로 같은 검사를 거친다. 원인 상세 기록은 복구 후 보완할 수 있다.

## 3. 버전

제품 변경 PR 하나를 출시 하나로 취급한다. 중간 커밋마다 올리지 않고, PR 최종 상태에서 출시 버전을 한 번 정한다.

- 앱 버전의 기준은 `package.json`의 안정 버전 `x.y.z`다. `package-lock.json`의 최상위와 루트 패키지 버전도 맞춘다.
- 기능 추가·개선과 새 문헌 추가는 **MINOR**, 버그 수정·기존 문헌 수정은 **PATCH**, 저장 데이터·백업·공개 동작의 비호환 변경은 **MAJOR**다. 여러 종류가 섞이면 가장 큰 변경을 따른다.
- 앱 코드, 실행·빌드 설정, 카탈로그, 폰트, 패키지·잠금 파일의 변경은 제품 변경으로 취급한다. 제품 변경 없이 설명 문서·테스트·CI·관리 도구만 바꾸면 앱 버전을 올리거나 배포하지 않는다. 빌드나 사용자 실행 결과에 영향을 주는 도구 변경은 제품 변경이다.
- 기존 문헌 내용이 바뀌면 해당 문헌의 개정 번호도 올린다. 앱 버전과 별개이며 문헌·카드 ID는 보존한다.
- 저장·백업 형식은 앱 버전과 별도로 호환성을 관리한다. 변경 시 읽을 수 있는 이전 형식, 이전 방법, 실패 시 보존·복구 방법을 기록한다.
- CI의 버전·경로 검사는 누락과 형식 오류를 잡는다. 기능 추가인지 수정인지의 의미와 올릴 버전 자리는 리뷰에서 판단한다.

출시한 버전은 다른 커밋에 재사용하지 않는다. 같은 커밋의 실패한 배포 재실행은 가능하다. 과거 이력은 확인 가능한 커밋·배포 기록으로 정리하며 소급해서 버전을 꾸미지 않는다.

## 4. 검증과 배포

모든 PR에서 필수 CI를 통과한다. 기본 검증은 `npm run lint`, `npm test`, `npm run build`이며 빌드에는 타입 검사도 포함된다. 변경에 맞는 동작 검증을 추가하고, 실행하지 못한 확인은 미검증으로 남긴다.

| 변경 | 추가 확인 |
| --- | --- |
| 코드·UI·입력 | 바뀐 동작과 회귀, 영향을 받는 PC·태블릿·폰 흐름 |
| 문헌·사전·폰트 | 출처·이용 근거, 내용·독음·글꼴, ID 보존과 개정 번호 |
| 저장·백업·이전 | 이전 데이터로 업그레이드·재시작·복원, 실패 시 데이터 보존 |
| 배포·캐시 | 신규 접속, 기존 PWA의 갱신, 오프라인 재접속 |

제품 변경이 `main`에 병합되면 자동 배포한다. 문서·테스트·CI·관리 도구만 바뀐 경우는 배포하지 않는다.

제품 배포 뒤에 문서 변경이 병합되어도 같은 제품 버전의 배포는 끝까지 진행한다. 더 새 제품 버전이 main에 있으면 이전 버전의 실행은 중단한다. 배포 대기는 workflow 전체를 `queue: max`로 직렬 처리한다(최대 100개). 실패한 제품 실행은 원래 실행을 재시도한다. 수동 실행은 main의 마지막 변경을 동일 규칙으로 검사하므로, 마지막 변경이 문서뿐이면 제품 배포를 생략한다.

배포 뒤에는 운영 URL, 표시 버전, 해당 변경의 실제 동작을 확인한다. `release.json`과 HTML·서비스 워커의 배포 식별자가 예상 커밋 SHA와 일치하는지 확인한다. HTTP 산출물 확인은 실제 기기의 UI·PWA 동작 검증을 대신하지 않는다. 위험에 맞는 위 표의 검사와 결과를 기록한다. CI 성공만으로 운영 동작이 정상이라고 쓰지 않는다.

제품 출시는 GitHub Release에 다음을 연결한다.

- 앱 버전과 `v<버전>` 태그, 실제 배포한 전체 커밋 SHA
- 변경 내용과 관련 이슈·PR
- 배포 실행 URL과 성공 결과
- 운영 확인 항목·결과, 미검증 사항과 알려진 제한

태그는 실제 배포 커밋을 가리켜야 한다. 실패하거나 필수 확인이 끝나지 않으면 이슈는 **배포 확인** 상태로 유지한다. Release와 검증 근거를 연결한 뒤 완료로 바꾸고 수동으로 닫는다.

복구 시 **이전 앱 배포**와 **사용자 데이터 복원**을 따로 판단한다. 이전 산출물·커밋과 저장 형식 호환성을 확인한다. 호환되지 않는 데이터에 이전 앱을 연결하는 식의 단순 되돌리기는 하지 않는다. 복구 원인, 사용한 버전, 데이터 영향과 확인 결과를 이슈에 남긴다.

## 5. 문서와 결정의 위치

| 위치 | 기준으로 삼는 정보 |
| --- | --- |
| Issues | 할 일, 우선순위, 상태, 완료 조건과 검증 근거 |
| GitHub Releases | 실제 출시한 변경과 배포·운영 확인 |
| `PROCESS.md` | 작업·검증·버전·배포 절차 |
| `SPEC.md` / `DESIGN.md` | 현재 기능·데이터 계약 / UI·상호작용 기준 |
| `ROADMAP.md` | 앞으로의 목표·우선순위·단계별 완료 조건 |
| `PROGRESS.md` | 다음 작업자를 위한 짧은 인계와 관련 링크 |
| `README.md` | 사용자·개발자 시작점과 문서 안내 |

중요한 결정은 이슈에 이유와 영향을 적고 관련 명세에 연결한다. 결정이 바뀌면 대체한 결정과 구현 작업을 연결한다. 목표와 현재 구현을 구분하며 같은 작업 목록을 여러 문서에 복제하지 않는다. `.omx/` 등의 AI 실행 자료는 보조 기록이며 공식 진행 상태의 기준이 아니다.

주 1회 우선순위와 멈춘 작업을 짧게 확인한다. 절차는 작은 실제 작업에 적용한 뒤 기록 부담과 누락 사례를 보고 조정한다.

문서를 바꿀 때는 현재 기준 문서에 반영하고 같은 내용을 별도 세션 로그로 복제하지 않는다. 대체된 설명은 필요한 결정·호환 근거를 남긴 뒤 삭제하며, 과거 원문은 Git 이력·이슈·PR로 조회한다. 삭제 전에 코드·빌드·문서의 참조와 출처·라이선스 보존 필요성을 확인하고 링크를 함께 갱신한다. 디자인 토큰과 글자 셀처럼 코드가 직접 참고하는 세부 기준은 간결하게 유지한다.
