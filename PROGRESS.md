# 文讀 — 프로젝트 현황 (세션 로그)

> 새 세션에서 이어받을 때 이 파일을 먼저 읽으세요.
> **현행 기능·데이터 명세와 개선 항목(P1~P12)은 `SPEC.md`**, 계획은 `ROADMAP.md`.
> 이 파일은 **시간순 세션 로그**만 담는다 (최신이 위). 2026-07-19 이전 로그는 `PROGRESS-archive.md`.
> 구조·스키마·기능·단축키 참조 섹션은 SPEC.md로 이관하고 삭제함 (2026-07-31 — 구판은 git 히스토리).

---

## 프로젝트 개요

한의학 한문 학습 웹앱 (PWA). **Vite + TypeScript + Tailwind CSS v4** (빌드 타임), 런타임 의존성 0.
콘텐츠 정본은 문헌별 JSON — `src/data/*.json`(빌드 시 번들 베이킹) + `catalog/*.json`(다운로드 배포).

- **실행 (관리자 저작)**: `문독.command` 더블클릭 → `server.py` (기본 http://localhost:19234, 점유 시 폴백 → `.runport`)
- **빌드**: `npm run build` (데이터 lint → tsc → vite) / **테스트**: `npm test`
- **배포**: `main` push → GitHub Actions → GitHub Pages — https://river11456.github.io/mundok/

---

## 📌 현재 상태 (2026-07-31) — 교재 카탈로그 배포·mode 화면 폐지 + interp 진행 중 (v1.17.1~v1.19.2)

### 배포된 것 (07-20 ~ 07-28)

| 버전 | 내용 |
|------|------|
| v1.17.1~4 (07-20) | 폴리시: 학습 화면 Space 힌트 정리(정답 보기 단독), 탭 제목·PWA 설치명 한글화(文讀→문독). 콘텐츠: 여담론 뒷부분 추가(精不足者/形不足者 대구·局方 온보 비판 단락) |
| v1.18.0~2 (07-27) | **문헌 진입 UX 통일** — 모든 문헌 클릭 = 상세 오버레이, `mode` 화면 폐지. 오버레이 safe center 정렬(v1.18.1), 주 모드를 순차 재생으로 — 버튼 순서·강조 및 `Enter`=순차/`⇧Enter`=안키(v1.18.2) |
| v1.19.0~2 (07-28) | **교재 신편의학한문 73문헌 카탈로그 배포** (sentence 레벨만 — 뜻·독음 0%, SPEC P10). 폰트 서브셋이 카탈로그 한자 누락하던 문제 수정(v1.19.1), lint가 미커밋 `src/interp.ts`를 참조해 배포 빌드가 깨지던 문제 수정(v1.19.2) |

### 미커밋 작업 (워킹트리, 2026-07-31 현재)

- **해석 순서(interp) 기능** — 편집(文 메뉴 `해석 순서 편집`, 드래그 토글)·재생(카드 도구 ▶). 신규 `src/interp.ts`·`src/interp-play.ts`·`test/interp.test.ts` + 15개 파일 수정(서버 API `save-interp`·스토리지·렌더·데이터 1건 포함). 정적 모드 델타는 `userdata.interp`(cardFront 키). **검증 후 커밋 필요** (v1.20.0 후보)
- **`src/data/textbook/`** — 교재 OCR 스테이징 73문헌 + `_manifest.json`(출처 메타). catalog/ 승격 완료로 역할 종료 — 단 `_manifest.json`의 원전·저자·교재 지면·검증 status는 **유일본**(SPEC P9), 카탈로그 스키마로 이전하기 전 삭제 금지

### 문서 정리 (07-29 ~ 07-31)

- `SPEC.md` 작성(07-29 as-is 전수 조사)·편입 — 구조 문제 P1~P12, v2 방향(8절) 확정
- `IMPROVEMENTS.md` 삭제 — 전 항목 완료 (잔여 '문독.bat Windows 검증'은 폐기). 개선 항목 관리는 SPEC 7절로 일원화
- PROGRESS를 세션 로그 전용으로 축소 — 과거 로그는 `PROGRESS-archive.md` 분리, stale 참조 섹션(파일 구조·스키마·기능·단축키) 삭제
- `ROADMAP.md` 재작성·`RENEWAL.md` 삭제 — 계획 문서 일원화 (미착수 D·E는 ROADMAP Phase 3·4로 이식). **8월 우선순위 확정**(사용자): Phase 3·4 → 동결·베타 유지, v2는 8월 설계만·구현은 개강 후. **2.0.0 기준 변경**: 리뉴얼 완료 시 → v2 완성 시 (CLAUDE.md 예외 조항 폐기)

### 다음 후보

1. interp 작업 검증·커밋 (v1.20.0)
2. Phase 3 착수 조건 확보 — Anthropic vision API 키 발급·1학기 PPT/써머리 샘플. **미확보 시 Phase 4(쓰기 모드) 먼저** (ROADMAP 참조)
3. v2 도메인 모델 설계 문서 (8월 문서만 — 구현·마이그레이션·백업 v3는 개강 후, ROADMAP Phase 6)

---

## 📌 이전 세션 (2026-07-19) — 콘텐츠 개방 3종: 셀 선택 저작·문헌 추가·카탈로그 (v1.15.0~v1.17.0)

사용자 논의로 설계 확정 후 순차 구현. **모두 커밋·배포 완료.**

| 커밋 | 내용 |
|------|------|
| v1.15.0 | **셀 기반 범위 선택 저작** — 카드 추가 텍스트 선택을 네이티브 선택(selectionchange·롱프레스)에서 글자 셀 포인터 스윕으로 전면 교체. 모드 진입 없는 디폴트 동작: 마우스·펜=즉시 긋기, 터치=220ms 홀드(떼면 한 글자) 또는 가로 우세 이동(세로는 스크롤 양보). 판정은 `select-gesture.ts` 순수 상태 기계(테스트 11건), 배선은 `cell-select.ts`. `#card-front` user-select:none + touch-callout 차단 — R7 선택 복제 트릭 제거 |
| v1.16.0 | **문헌 추가** — `hanja-v2/user-docs`(DocJSON 배열, `u1…` id, updatedAt) + 홈 '내 문헌' 선반·새 문헌 타일. 마법사: 제목·부제·표지색 → 본문 붙여넣기 → 자동 분할(줄바꿈 우선, 없으면 종결부호) 미리보기 → 일괄 생성. 사용자 문헌 카드 CRUD·문법은 델타 아닌 **id 기반 직접 수정**(LocalStore 분기). 관리자 모드는 `/api/create-doc`이 `src/data/*.json` 생성(curl 5케이스 검증). 상세 오버레이에 본문 추가·정보 수정·삭제 |
| v1.17.0 | **문헌 카탈로그** — `catalog/*.json` → 빌드 시 `dist/catalog/`(index.json 포함, build-catalog vite 플러그인) → Pages 동일 origin 배포. 홈 '문헌 받기' 모달: 받기·업데이트·설치됨 상태, SW 캐시는 ?v= 쿼리 버스팅. 설치본 = source 메타 붙은 user-doc, 업데이트는 카드 id 유지 통째 교체(학습기록 보존). lint가 카탈로그도 검사. 카탈로그는 이 시점 비어 있음(→ v1.19.0에서 73문헌 등록) |

- 검증: 테스트 69건(신규 20)·tsc·빌드·lint 통과, create-doc 실서버 curl 검증
- **설계 결정** (사용자): 셀 선택은 아이콘 진입 없이 디폴트 / 데스크톱도 셀 선택으로 통일 / 붙여넣기 마법사+단건 병행 / 베이킹 문헌 8개는 유지, **2.0에서 카탈로그로 완전 이전(굿노트형)**
- **서비스화 검토 결론** (사용자 질문): 네이티브 앱 기각(배포 속도 상실 > 실익 0) — PWA 유지. 동기화는 필요 시 `BackendStore`+BaaS(Supabase류)로, user-docs의 문헌 단위 updatedAt이 그 대비. 배포 시 홈 화면 설치(PWA) 유도 온보딩 권장
- ⚠ **잔여 확인**: 아이패드 실기기 — **셀 선택**(홀드·가로 긋기·펜슬), 새 문헌 마법사 터치, 카탈로그 모달. 데스크톱 육안 — 선택 하이라이트·버블 위치

---

> 2026-07-19 이전 세션 로그(리뉴얼 Phase 1~2, 반응형·터치, CSV→JSON 마이그레이션 등): **`PROGRESS-archive.md`**
